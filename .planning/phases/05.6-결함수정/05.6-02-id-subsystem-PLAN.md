---
phase: 05.6-결함수정
plan: 02
type: execute
wave: 2
depends_on:
  - 05.6-01
files_modified:
  - Forms/MainForm.cs                 # lines 978-1000 (Event ID 경고 추가), 1471-1473 (BBOX 생성 시 ID 승계), 2317-2507 (KeyDown/ChangeBoxIdWithinWaypoint), 2742-2828 (UpdateBboxPanel Person UI)
autonomous: true
requirements:
  - NEW-01
  - NEW-02
  - NEW-03
  - NEW-04
  - NEW-05
  - NEW-06
  - NEW-07
execution_note: "Wave 2 (after 05.6-01). Serial dependency chain on Forms/MainForm.cs. 4 tasks exceed soft 2-3 guideline by design per D-19 (ID subsystem 재설계는 묶어서 처리해야 상호 의존성이 풀림)."

must_haves:
  truths:
    - "ROADMAP-SC-02: BBOX 생성 → 사후 ID 지정 → Entry 설정 후 Exit 프레임 BBOX 생성 시 Entry 지정 ID가 유지된다"
    - "ROADMAP-SC-03: 선택된 개별 BBOX의 ID만 변경되고, 같은 클래스·같은 ID의 다른 박스는 영향받지 않는다"
    - "ROADMAP-SC-04: Ctrl+N 입력 시 Exit 프레임 BBOX의 ID가 Entry 프레임 BBOX의 ID와 자동 일치된다"
    - "ROADMAP-SC-05: Entry-Exit ID 불일치 시 Person/Vehicle/Event 모두 경고 메시지가 표시된다"
    - "ListView/Label/ComboBox에 포커스가 있어도 Ctrl+1~0, Ctrl+N 단축키가 정상 동작한다"
    - "Person 클래스 사이드바에서 ID 를 1~20 범위로 수동 변경할 수 있다"
  artifacts:
    - path: "Forms/MainForm.cs"
      provides: "ChangeBoxIdOnly 신규 메서드 (개별 박스 전용), Ctrl+N 핸들러, Event ID 경고 블록, Person NumericUpDown UI, BBOX 생성 시 ID 승계 로직"
      contains: "ChangeBoxIdOnly|Keys.N|entryEventBoxes|Person.*NumericUpDown"
  key_links:
    - from: "MainForm_KeyDown Ctrl+1~0 branches (line 2347-2411)"
      to: "ChangeBoxIdOnly (신규) / currentAssignedId"
      via: "selectedBox 가 있으면 ChangeBoxIdOnly(selectedBox, newId) — ChangeBoxIdWithinWaypoint 대체"
      pattern: "ChangeBoxIdOnly\\(selectedBox"
    - from: "MainForm_KeyDown Ctrl+N (신규 추가)"
      to: "Entry 프레임 ID → Exit 프레임 BBOX ID 일괄 매칭 로직"
      via: "Keys.N && e.Control 조건 브랜치 신설"
      pattern: "e\\.Control.*Keys\\.N"
    - from: "SetExitMarkerAndCreateWaypoint ID 불일치 검증 (line 978-991)"
      to: "Event 클래스 불일치 검증 블록 추가"
      via: "exitEventBoxes 변수 + foreach 루프 추가"
      pattern: "exitEventBoxes"
    - from: "BBOX 생성 로직 (line 1471-1473)"
      to: "ID 승계 (selectedBox 또는 currentAssignedId)"
      via: "생성 직후 같은 클래스 selectedBox 가 있으면 ID 승계; Exit 프레임이면 Entry 프레임 ID 조회"
      pattern: "// NEW-01.*ID 승계"
    - from: "UpdateBboxPanel Person 분기 (line 2810-2823)"
      to: "NumericUpDown 컨트롤 추가 (Min=1, Max=20)"
      via: "Label 대신 NumericUpDown + ValueChanged → ChangeBoxIdOnly"
      pattern: "NumericUpDown.*Minimum = 1.*Maximum = 20"
    - from: "MainForm_KeyDown ActiveControl 가드 (line 2332-2336)"
      to: "Ctrl+숫자 / Ctrl+N 예외 허용"
      via: "e.Control && (숫자키 | Keys.N) 면 가드 통과"
      pattern: "e\\.Control && \\(IsIdAssignmentKey"
---

<objective>
NEW-01 ~ NEW-07: ID 관리 서브시스템 전면 재설계.

**해결할 버그 7종**:
1. NEW-01: BBOX 생성 후 사후 ID 지정 → Exit 프레임에서 새 BBOX 생성 시 Entry ID 유지 실패 → BBOX 생성 시 ID 승계 로직 추가.
2. NEW-02: Entry-Exit ID 불일치 경고에 Event 클래스 누락 → Event 검증 블록 추가.
3. NEW-03: Ctrl+N 유령 단축키 → Exit 프레임 BBOX ID 를 Entry 프레임과 자동 매칭시키는 기능으로 신규 구현 (이미 오류 메시지 line 996 에서 홍보됨).
4. NEW-04: TextBox/ComboBox 포커스 시 Ctrl+1~0 단축키 먹통 → ActiveControl 가드를 예외 처리.
5. NEW-05 + NEW-07: `ChangeBoxIdWithinWaypoint` 가 같은 Waypoint 내 동일 ID 박스 **전부** 변경 → 선택된 개별 박스만 변경하는 `ChangeBoxIdOnly` 신규 메서드로 대체.
6. NEW-06: Person 사이드바에 ID 변경 UI 부재 → NumericUpDown(Min=1, Max=20) 추가.

Purpose: ISO/IEC 25023 기능적정성/사용성/신뢰성 복합 결함. QA팀이 발견한 핵심 블로커. 이 플랜 미완료 시 GS인증 1등급 불가.
Output: `Forms/MainForm.cs` 에 새 메서드 `ChangeBoxIdOnly`, Ctrl+N 핸들러, Event 불일치 검증, Person NumericUpDown UI, BBOX 생성 ID 승계 로직이 모두 추가된다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.6-결함수정/05.6-CONTEXT.md
@.planning/DEFECTS-INBOX.md
@Forms/MainForm.cs

<interfaces>
From Forms/MainForm.cs (line 2491-2507 — 현재 버그의 본체):
```csharp
private void ChangeBoxIdWithinWaypoint(BoundingBox box, int newId)
{
    int oldId = GetBoxId(box);
    var waypoint = FindWaypointForBox(box);
    if (waypoint != null)
    {
        // ❌ BUG: 같은 Label+oldId 의 모든 박스를 업데이트 → NEW-05/07
        var boxesToUpdate = boundingBoxes.Where(b =>
            b.Label == box.Label && GetBoxId(b) == oldId &&
            b.FrameIndex >= waypoint.EntryFrame && b.FrameIndex <= waypoint.ExitFrame &&
            !b.IsDeleted && FindWaypointForBox(b) == waypoint).ToList();
        foreach (var b in boxesToUpdate) SetBoxId(b, box.Label, newId);
        waypoint.ObjectId = newId;
        UpdateWaypointListView();
    }
    else { SetBoxId(box, box.Label, newId); }
    UpdateObjectInfo(box); UpdateBboxListDisplay(); pictureBoxVideo.Invalidate();
}
```

From Forms/MainForm.cs (line 978-999 — Event 누락 확인):
```csharp
// FUNC-06: Entry-Exit 프레임 간 객체 ID 불일치 감지 및 경고
var exitPersonBoxes = GetBboxesForFrame(currentFrameIndex).Where(b => b.Label == "person" && !b.IsDeleted).ToList();
var exitVehicleBoxes = GetBboxesForFrame(currentFrameIndex).Where(b => b.Label == "vehicle" && !b.IsDeleted).ToList();
// ❌ exitEventBoxes 가 없음
var mismatchMessages = new List<string>();
foreach (var entryBox in entryPersonBoxes) { ... Person 검증 ... }
foreach (var entryBox in entryVehicleBoxes) { ... Vehicle 검증 ... }
// ❌ Event foreach 루프 없음 → NEW-02
```

From Forms/MainForm.cs (line 2332-2336 — NEW-04 원인):
```csharp
Control focusedControl = this.ActiveControl;
if (focusedControl is TextBox || focusedControl is ComboBox)
{
    if (e.KeyCode != Keys.Enter && e.KeyCode != Keys.Escape) return;  // ❌ Ctrl+숫자/N 도 차단
}
```

From Forms/MainForm.cs (line 2810-2823 — Person 사이드바 UI 현재):
```csharp
else // person 분기 → Label 만 존재, 편집 불가 (NEW-06)
{
    var label = new Label { Text = text, AutoSize = false, Dock = DockStyle.Fill, ... };
    label.Click += (s, ev) => { selectedBox = capturedBox; ... };
    itemPanel.Controls.Add(label);
}
```

From Forms/MainForm.cs (line 1471-1473 — BBOX 생성 시 ID 할당):
```csharp
PersonId = currentSelectedLabel == "person" ? currentAssignedId : 0,
VehicleId = currentSelectedLabel == "vehicle" ? currentAssignedId : 0,
EventId = currentSelectedLabel == "event" ? currentAssignedId : 0,
```
— `currentAssignedId` 가 사용되지만 Entry 프레임에 기존 박스가 있어도 조회하지 않음 → NEW-01.

Helpers (이미 존재):
- `int GetBoxId(BoundingBox box)` — Person/Vehicle/EventId 를 Label 기준으로 반환
- `void SetBoxId(BoundingBox box, string label, int id)` — 역방향
- `WaypointMarker FindWaypointForBox(BoundingBox box)` — box 를 포함하는 waypoint 조회
- `int? GetIdFromKey(Keys keyCode, int offset)` (line 2476-2489) — D1~D0 → 1~10 반환
- `int? entryFrameIndex` (필드)
- `List<WaypointMarker> waypointMarkers` (필드)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ChangeBoxIdOnly 신규 메서드 + Ctrl+1~0 / Alt+1~0 모든 분기를 새 메서드로 교체 (NEW-05, NEW-07)</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-02 항목)
    - Forms/MainForm.cs lines 2347-2411 (Ctrl+1~0 Person/Vehicle/Event 분기 전체)
    - Forms/MainForm.cs lines 2491-2507 (ChangeBoxIdWithinWaypoint 원본)
    - Forms/MainForm.cs line 2805 (UpdateBboxPanel ComboBox.SelectedIndexChanged 에서 ChangeBoxIdWithinWaypoint 호출하는 곳)
  </read_first>
  <behavior>
    - 선택된 개별 BBOX 의 ID만 변경됨. 같은 Waypoint 내 같은 Label+oldId 의 다른 박스는 변경되지 않음.
    - Waypoint.ObjectId 는 변경하지 않음 (Waypoint 단위 일괄 변경은 사용자가 Waypoint 전체 삭제 후 재생성해야 함 — 의도적 보수성).
    - Undo 스택에 등록되어 Ctrl+Z 로 복원 가능.
    - 기존 `ChangeBoxIdWithinWaypoint` 메서드는 **제거 또는 `[Obsolete]` 마킹**. 호출처 모두 `ChangeBoxIdOnly` 로 전환.
  </behavior>
  <action>
1. `Forms/MainForm.cs` 에 신규 메서드 추가 (기존 `ChangeBoxIdWithinWaypoint` 바로 위 또는 아래 배치, line 2491 근처):
```csharp
/// <summary>
/// D-02 (NEW-05/07): 선택된 개별 BBOX 하나만 ID 변경. 같은 Waypoint 내 동일 ID 다른 박스는 영향받지 않음.
/// </summary>
private void ChangeBoxIdOnly(BoundingBox box, int newId)
{
    if (box == null) return;
    int oldId = GetBoxId(box);
    if (oldId == newId) return;

    // Undo 등록 (AddUndoAction 패턴 유지 — 04 Phase 결정 사항)
    AddUndoAction(new UndoAction
    {
        Type = UndoActionType.ModifyBox,
        Box = CloneBoundingBox(box),  // 변경 전 상태 저장
    });

    SetBoxId(box, box.Label, newId);

    // NEW-05 수정: Waypoint.ObjectId 는 자동 변경하지 않음
    // (Waypoint 전체를 바꾸려면 사용자가 Waypoint 를 삭제 후 재생성)

    UpdateObjectInfo(box);
    UpdateBboxListDisplay();
    pictureBoxVideo.Invalidate();
}
```

2. `Forms/MainForm.cs:2354` 의 `ChangeBoxIdWithinWaypoint(selectedBox, assignedId.Value);` 를 `ChangeBoxIdOnly(selectedBox, assignedId.Value);` 로 변경.

3. `Forms/MainForm.cs:2367` 의 Alt+1~0 Person 분기도 동일하게 `ChangeBoxIdOnly` 로 변경.

4. `Forms/MainForm.cs:2379` 의 Vehicle selectedBox 분기를 `ChangeBoxIdOnly(selectedBox, classId.Value);` 로 변경.

5. `Forms/MainForm.cs:2399` 의 Event selectedBox 분기를 `ChangeBoxIdOnly(selectedBox, classId.Value);` 로 변경.

6. `Forms/MainForm.cs:2805` 의 `UpdateBboxPanel` ComboBox SelectedIndexChanged 핸들러도 `ChangeBoxIdOnly(capturedBox, newClassId);` 로 변경.

7. 기존 `ChangeBoxIdWithinWaypoint` 메서드는 **제거**. (호출처가 모두 `ChangeBoxIdOnly` 로 전환되면 미사용 코드)
   - 만약 Waypoint 단위 일괄 변경이 다른 곳(예: DF-1-03 Waypoint 삭제 플로우, 05.6-03 Plan)에서 필요하다면, 거기서 별도 `ChangeAllBoxesInWaypoint` 를 신규 도입. 본 플랜은 제거 지향.

8. `UndoActionType.ModifyBox` 가 존재하는지 확인. 없으면 enum 에 추가하고 Undo/Redo 처리에 `case ModifyBox: ...` 브랜치 추가. 이미 있으면 재사용.
  </action>
  <verify>
    <automated>
      cd "C:/Users/ANNA/AOLTv1.0" && dotnet build ASLTv1.0.csproj -c Debug -v q 2>&1 | tail -20
      # ChangeBoxIdWithinWaypoint 호출 0건 검증:
      grep -c "ChangeBoxIdWithinWaypoint" Forms/MainForm.cs
      # 0 이어야 함 (제거 완료)
      grep -c "ChangeBoxIdOnly" Forms/MainForm.cs
      # 최소 6건 (정의 1 + 호출 5 이상)
      # 새 메서드에 .Where() over boundingBoxes 가 없는지 (= 전체 스캔 안 함):
      grep -A 15 "private void ChangeBoxIdOnly" Forms/MainForm.cs | grep -c "boundingBoxes\.Where"
      # 0 이어야 함
    </automated>
  </verify>
  <acceptance_criteria>
    - `Forms/MainForm.cs` 에 `private void ChangeBoxIdOnly` 메서드 정의 1건 존재.
    - `Forms/MainForm.cs` 에 `ChangeBoxIdWithinWaypoint` 호출 또는 정의 0건 (완전 제거).
    - `ChangeBoxIdOnly` 메서드 본문에 `boundingBoxes.Where` 패턴 0건 (전체 리스트 순회 없음).
    - `ChangeBoxIdOnly` 메서드 본문에 `waypoint.ObjectId =` 패턴 0건 (Waypoint 변경 없음).
    - `ChangeBoxIdOnly` 호출 지점 최소 5곳 (Ctrl Person, Alt Person, Ctrl Vehicle, Ctrl Event, ComboBox SelectedIndexChanged).
    - `dotnet build` 성공.
  </acceptance_criteria>
  <done>
    UAT 영상 `[5].동일 클래스 다중 객체 ID 변경.mp4` 재현 시나리오에서 단축키로 ID 변경 시 선택된 박스만 ID 가 바뀌고 다른 박스는 원래 ID 유지.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ctrl+N 신규 구현 (NEW-03) + Event 클래스 ID 불일치 경고 추가 (NEW-02) + ActiveControl 포커스 가드 예외 (NEW-04)</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-01, D-03, D-04)
    - .planning/DEFECTS-INBOX.md (NEW-02, NEW-03, NEW-04 섹션)
    - Forms/MainForm.cs lines 978-999 (ID 불일치 검증 현재 Person/Vehicle 만 있는 블록)
    - Forms/MainForm.cs lines 2330-2345 (MainForm_KeyDown 진입부 ActiveControl 가드)
    - Forms/MainForm.cs lines 2476-2489 (GetIdFromKey 헬퍼)
    - Forms/MainForm.cs line 996 (기존 "Ctrl+N 또는 단축키로" 문구 — 이것이 실구현의 근거)
  </read_first>
  <behavior>
    - **Ctrl+N**: 현재 프레임이 어떤 Waypoint 의 Exit 프레임이거나 Exit 프레임 후보(사용자가 Exit 마커를 찍으려는 시점)일 때, 현재 프레임의 모든 BBOX(Person/Vehicle/Event) 를 해당 Waypoint 의 Entry 프레임에서 같은 Label 로 존재하는 박스의 ID 로 자동 일치시킨다.
      - 단순화 구현: `selectedBox` 가 있으면 그 selectedBox 와 같은 Label 인 Entry 프레임 박스(첫 번째 일치)의 ID 로 selectedBox 를 매칭. selectedBox 가 없으면 현재 프레임의 모든 박스를 Entry 프레임 동일 Label 첫 번째 박스 ID 로 매칭.
      - Entry 프레임 인덱스 결정 로직: `entryFrameIndex` 필드가 있으면 그 값 사용. 없으면 현재 프레임을 포함하는 Waypoint 의 `EntryFrame` 사용. 둘 다 없으면 한국어 메시지 `"Entry 프레임이 지정되지 않아 Ctrl+N 을 수행할 수 없습니다."` 표시 후 return.
    - **Event 불일치 경고**: Person/Vehicle 과 동일 포맷으로 Event 추가. `$"Event ID {entryBox.EventId:D2}: Entry 프레임({entryFrameIndex.Value})과 Exit 프레임({currentFrameIndex}) 간 ID가 일치하지 않습니다."`
    - **포커스 가드 예외**: Ctrl+숫자(D0~D9, NumPad0~9) 또는 Ctrl+N 은 TextBox/ComboBox/ListView 포커스 상태에서도 통과.
  </behavior>
  <action>
1. **NEW-04 (포커스 가드 예외)** — `Forms/MainForm.cs:2330` `MainForm_KeyDown` 진입부를 다음과 같이 수정:
```csharp
private void MainForm_KeyDown(object sender, KeyEventArgs e)
{
    Control focusedControl = this.ActiveControl;
    if (focusedControl is TextBox || focusedControl is ComboBox)
    {
        // NEW-04: Ctrl+숫자(D0~D9, NumPad0~9), Ctrl+N 은 ID 지정 단축키이므로 포커스와 무관하게 통과
        bool isIdShortcut = e.Control && (IsIdAssignmentKey(e.KeyCode) || e.KeyCode == Keys.N);
        if (!isIdShortcut && e.KeyCode != Keys.Enter && e.KeyCode != Keys.Escape) return;
    }
    // ... 기존 코드 이어짐
}
```

2. 헬퍼 메서드 신규 추가 (MainForm 클래스 private static 또는 private):
```csharp
/// <summary>NEW-04: ID 지정용 숫자 키인지 판정 (Ctrl+D0~D9, Ctrl+NumPad0~9)</summary>
private static bool IsIdAssignmentKey(Keys keyCode)
{
    return (keyCode >= Keys.D0 && keyCode <= Keys.D9)
        || (keyCode >= Keys.NumPad0 && keyCode <= Keys.NumPad9);
}
```

3. **NEW-03 (Ctrl+N 신규 구현)** — `Forms/MainForm.cs:2411` (Event 브랜치 끝) 뒤, `if (!_videoService.IsVideoLoaded) return;` (line 2413) **앞에** 신규 Ctrl+N 핸들러 추가:
```csharp
// NEW-03: Ctrl+N — Exit 프레임 BBOX ID 를 Entry 프레임과 자동 매칭
if (e.Control && !e.Shift && !e.Alt && e.KeyCode == Keys.N)
{
    if (!_videoService.IsVideoLoaded) { e.Handled = true; return; }

    // Entry 프레임 결정
    int? entryFrame = null;
    if (entryFrameIndex.HasValue) entryFrame = entryFrameIndex.Value;
    else
    {
        int curFrame = _videoService.CurrentFrameIndex;
        var containing = waypointMarkers.FirstOrDefault(w => curFrame >= w.EntryFrame && curFrame <= w.ExitFrame);
        if (containing != null) entryFrame = containing.EntryFrame;
    }
    if (!entryFrame.HasValue)
    {
        MessageBox.Show("Entry 프레임이 지정되지 않아 Ctrl+N 을 수행할 수 없습니다.", "알림",
            MessageBoxButtons.OK, MessageBoxIcon.Information);
        e.Handled = true; return;
    }

    int currentFrame = _videoService.CurrentFrameIndex;
    var entryBoxes = GetBboxesForFrame(entryFrame.Value).Where(b => !b.IsDeleted).ToList();
    var exitBoxes = GetBboxesForFrame(currentFrame).Where(b => !b.IsDeleted).ToList();

    int matched = 0;
    if (selectedBox != null && exitBoxes.Contains(selectedBox))
    {
        // 선택된 박스만 매칭 (selectedBox Label 과 같은 Entry 박스 첫 번째 ID 사용)
        var entryMatch = entryBoxes.FirstOrDefault(b => b.Label == selectedBox.Label);
        if (entryMatch != null && GetBoxId(entryMatch) != GetBoxId(selectedBox))
        {
            ChangeBoxIdOnly(selectedBox, GetBoxId(entryMatch));
            matched++;
        }
    }
    else
    {
        // 현재 프레임의 모든 Exit 박스를 Label 기준으로 매칭
        foreach (var exitBox in exitBoxes)
        {
            var entryMatch = entryBoxes.FirstOrDefault(b => b.Label == exitBox.Label);
            if (entryMatch != null && GetBoxId(entryMatch) != GetBoxId(exitBox))
            {
                ChangeBoxIdOnly(exitBox, GetBoxId(entryMatch));
                matched++;
            }
        }
    }

    if (matched == 0)
        MessageBox.Show("일치시킬 ID 차이가 없습니다.", "알림", MessageBoxButtons.OK, MessageBoxIcon.Information);
    else
        pictureBoxVideo.Invalidate();

    e.Handled = true; return;
}
```

4. **NEW-02 (Event 불일치 경고)** — `Forms/MainForm.cs:980` 뒤에 Event 박스 수집과 foreach 루프 추가:
```csharp
// NEW-02: Event 클래스도 Person/Vehicle 과 동일하게 Entry-Exit ID 불일치 검증
var exitEventBoxes = GetBboxesForFrame(currentFrameIndex).Where(b => b.Label == "event" && !b.IsDeleted).ToList();
var entryEventBoxes = GetBboxesForFrame(entryFrameIndex.Value).Where(b => b.Label == "event" && !b.IsDeleted).ToList();

// ... 기존 Person/Vehicle foreach 아래에 다음 추가:
foreach (var entryBox in entryEventBoxes)
{
    if (exitEventBoxes.Count > 0 && !exitEventBoxes.Any(b => b.EventId == entryBox.EventId))
        mismatchMessages.Add($"Event ID {entryBox.EventId:D2}: Entry 프레임({entryFrameIndex.Value})과 Exit 프레임({currentFrameIndex}) 간 ID가 일치하지 않습니다.");
}
```

5. **Ctrl+N 문구 보존**: `Forms/MainForm.cs:996` 의 "해결 방법: Ctrl+N 또는 단축키로 Exit 프레임의 객체 ID를 Entry 프레임과 일치시킨 후 다시 시도하세요." 메시지는 **그대로 유지** (이제 실구현됨).
  </action>
  <verify>
    <automated>
      cd "C:/Users/ANNA/AOLTv1.0" && dotnet build ASLTv1.0.csproj -c Debug -v q 2>&1 | tail -20
      # Ctrl+N 브랜치 검증:
      grep -n "Keys\.N" Forms/MainForm.cs
      # 최소 1건 (e.KeyCode == Keys.N 포함)
      # Event 불일치 검증:
      grep -n "entryEventBoxes" Forms/MainForm.cs
      # 최소 2건 (선언 + 사용)
      grep -n "Event ID.*불일치" Forms/MainForm.cs
      # 최소 1건 (경고 메시지 포맷)
      # 포커스 가드 헬퍼:
      grep -n "IsIdAssignmentKey" Forms/MainForm.cs
      # 최소 2건 (정의 + 호출)
    </automated>
  </verify>
  <acceptance_criteria>
    - `Forms/MainForm.cs` 에 `e.KeyCode == Keys.N` (또는 동등) 이 `e.Control && ... && e.KeyCode == Keys.N` 패턴으로 등장.
    - `Forms/MainForm.cs` 에 `exitEventBoxes` 와 `entryEventBoxes` 변수 선언 존재.
    - `Forms/MainForm.cs` 에 `"Event ID {...:D2}: Entry 프레임"` 와 호환되는 경고 메시지 생성 라인 존재 (grep `Event ID.*Entry 프레임.*불일치`).
    - `Forms/MainForm.cs` 에 `IsIdAssignmentKey` 메서드 정의 + MainForm_KeyDown 진입부에서 호출 존재.
    - MainForm_KeyDown 진입부의 기존 `if (focusedControl is TextBox || focusedControl is ComboBox)` 블록이 Ctrl+숫자/N 예외 로직을 포함 (`isIdShortcut` 또는 동등 변수).
    - `Forms/MainForm.cs:996` 의 "Ctrl+N" 문구 그대로 유지됨 (grep `Ctrl\+N 또는 단축키로`).
    - `dotnet build` 성공.
  </acceptance_criteria>
  <done>
    UAT: (A) Exit 프레임에서 Ctrl+N 입력 → Entry ID 자동 매칭 동작. (B) Event 박스 Entry-Exit ID 다르게 배치 + Exit 설정 → Event 불일치 경고 메시지 표시. (C) ComboBox 포커스 상태에서 Ctrl+3 입력 → ID 할당됨.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Person 사이드바 ID 변경 UI 추가 (NEW-06) — NumericUpDown Min=1 Max=20</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-05)
    - .planning/DEFECTS-INBOX.md (NEW-06 — `[4].Person 수동 변경 아직안됨.png`)
    - Forms/MainForm.cs lines 2753-2828 (UpdateBboxPanel 메서드 전체)
  </read_first>
  <behavior>
    - Person 클래스 BBOX 리스트 아이템에 NumericUpDown 컨트롤이 렌더링된다.
    - NumericUpDown 범위: Min=1, Max=20 (Person ID 는 Ctrl+1~0 + Alt+1~0 으로 1~20).
    - 값 변경 시 해당 박스의 PersonId 만 변경 (ChangeBoxIdOnly 호출, 다른 박스 영향 없음).
    - 기존 Label 클릭 → selectedBox 설정 동작도 유지.
    - Designer 파일 수정 불필요 (UpdateBboxPanel 이 런타임에 컨트롤 동적 생성).
  </behavior>
  <action>
1. `Forms/MainForm.cs:2810` 의 `else` 블록 (Person 분기)을 다음과 같이 교체:
```csharp
else
{
    // NEW-06 (D-05): Person 분기도 Vehicle/Event 처럼 ID 변경 UI 제공
    // Person ID 는 1~20 범위 (Ctrl+1~0, Alt+1~0) → NumericUpDown 사용
    int currentId = GetBoxId(capturedBox);

    // Label (클릭 영역 확보) — 왼쪽 70%
    var label = new Label
    {
        Text = text,
        AutoSize = false,
        ForeColor = DarkTheme.TextPrimary,
        Font = new Font("Segoe UI", 8F),
        TextAlign = ContentAlignment.MiddleLeft,
        Padding = new Padding(5, 0, 0, 0),
        Dock = DockStyle.Fill
    };

    // NumericUpDown — 오른쪽 도킹
    var numericId = new NumericUpDown
    {
        Minimum = 1,
        Maximum = 20,
        Value = Math.Max(1, Math.Min(20, currentId)),
        Width = 50,
        Dock = DockStyle.Right,
        BackColor = DarkTheme.Panel,
        ForeColor = DarkTheme.TextPrimary,
        BorderStyle = BorderStyle.None,
        TabStop = false,
        Font = new Font("Segoe UI", 8F)
    };
    numericId.ValueChanged += (s, ev) =>
    {
        int newId = (int)numericId.Value;
        if (newId != GetBoxId(capturedBox))
        {
            selectedBox = capturedBox;
            ChangeBoxIdOnly(capturedBox, newId);
        }
    };

    label.Click += (s, ev) => { selectedBox = capturedBox; UpdateObjectInfo(capturedBox); HighlightSelectedBoxInSidebar(); pictureBoxVideo.Invalidate(); };
    itemPanel.Click += (s, ev) => { selectedBox = capturedBox; UpdateObjectInfo(capturedBox); HighlightSelectedBoxInSidebar(); pictureBoxVideo.Invalidate(); };

    itemPanel.Controls.Add(label);      // Fill
    itemPanel.Controls.Add(numericId);  // DockStyle.Right 이므로 추가 순서 상관없이 오른쪽 배치 (Fill 앞에 추가 주의)
}
```

2. 단, WinForms 도킹 순서: `DockStyle.Right` 컨트롤을 **먼저** 추가해야 `DockStyle.Fill` 이 나머지 영역을 점유. 위 코드 그대로면 Fill 이 먼저 추가되어 Right 공간을 침범할 수 있으므로, 컨트롤 추가 순서를 다음으로 변경:
```csharp
itemPanel.Controls.Add(numericId);  // Right 먼저
itemPanel.Controls.Add(label);      // Fill 나중
```

3. Person Label 폭은 Vehicle/Event ComboBox 와 일관성을 위해 기존 `panel.Width - 20` 유지.
  </action>
  <verify>
    <automated>
      cd "C:/Users/ANNA/AOLTv1.0" && dotnet build ASLTv1.0.csproj -c Debug -v q 2>&1 | tail -20
      # Person 분기에 NumericUpDown 추가 확인:
      grep -B 2 -A 20 "else // person 분기\|else\s*$" Forms/MainForm.cs | grep -c "NumericUpDown"
      # 최소 1건
      # Min=1, Max=20 값 검증:
      grep -n "Minimum = 1" Forms/MainForm.cs
      grep -n "Maximum = 20" Forms/MainForm.cs
      # Person 분기에서 ChangeBoxIdOnly 호출:
      grep -B 5 "numericId\.ValueChanged" Forms/MainForm.cs
    </automated>
    <manual>
      수동 UAT: 앱 실행 → 영상 로드 → Person BBOX 그리기 → 우측 Person Labels 영역에 NumericUpDown 표시 확인 → 숫자 변경 시 해당 박스 ID 만 변경, 다른 Person 박스 ID 는 그대로.
    </manual>
  </verify>
  <acceptance_criteria>
    - `Forms/MainForm.cs` UpdateBboxPanel 메서드 내 Person 분기에 `NumericUpDown` 타입 인스턴스화 코드 1건 이상.
    - NumericUpDown 의 `Minimum = 1` 과 `Maximum = 20` 설정 존재.
    - NumericUpDown 의 `ValueChanged` 이벤트 핸들러가 `ChangeBoxIdOnly(capturedBox, newId)` 호출 패턴 포함.
    - Vehicle / Event 분기는 기존대로 ComboBox 유지 (regression 없음 — grep `ComboBox` 개수 유지).
    - `dotnet build` 성공.
  </acceptance_criteria>
  <done>
    UAT 재현 시 Person 사이드바에 NumericUpDown 노출 + ID 변경 시 개별 박스만 변경.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: BBOX 생성 시 ID 승계 로직 추가 (NEW-01)</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-06)
    - .planning/DEFECTS-INBOX.md (NEW-01, QA 영상 `[1, 4] 수정 사항 반영 안됨.mp4`)
    - Forms/MainForm.cs lines 1460-1490 (BBOX 생성 경로 — `boundingBoxes.Add(new BoundingBox { ... })`)
    - Forms/MainForm.cs lines 420-470 (또 다른 BBOX 생성 경로 — draw complete 시)
  </read_first>
  <behavior>
    - 사용자가 BBOX 를 새로 그릴 때 (MouseUp):
      1. 먼저 `currentAssignedId` (사전 지정 ID) 가 유효하면 그 값 사용.
      2. 아니면, 현재 프레임이 **기존 Waypoint 의 ExitFrame 범위 내부**이고 Entry 프레임에 같은 Label 의 박스가 있으면 그 Entry 박스 ID 를 승계.
      3. 아니면, 현재 프레임에 이미 같은 Label 의 selectedBox 가 있으면 그 ID 승계.
      4. 아니면, 기본 ID 사용 (person_01 / car / event_hazard).
    - 기존 `currentAssignedId == 1` (기본값) 상황에서도 자동으로 Entry 박스 ID 로 덮어써야 NEW-01 해결.
  </behavior>
  <action>
1. `Forms/MainForm.cs:1460-1490` 구간의 BBOX 생성 지점(들)을 찾아 `PersonId = currentSelectedLabel == "person" ? currentAssignedId : 0,` 계열 라인 **직전**에 ID 승계 로직 삽입. 구체 위치를 grep 으로 먼저 확정:
   - grep `new BoundingBox\s*{` Forms/MainForm.cs 로 모든 BBOX 생성 지점 수집 (draw complete 경로만 적용, 복제/undo 경로는 제외).

2. 다음 헬퍼 메서드를 `MainForm` 에 추가:
```csharp
/// <summary>
/// NEW-01 (D-06): 새 BBOX 생성 시 사용할 ID 결정.
/// 우선순위: (1) currentAssignedId 사용자 지정 (기본값 1 이 아니거나 명시 지정) → (2) 해당 프레임이 속한 Waypoint 의 Entry 프레임에 같은 Label 박스가 있으면 그 ID → (3) 현재 프레임의 같은 Label selectedBox ID → (4) 기본값 1.
/// </summary>
private int ResolveIdForNewBox(string label, int currentFrameIndex)
{
    // Waypoint 가 포함하는 범위면 Entry 프레임 ID 승계
    var containingWaypoint = waypointMarkers
        .FirstOrDefault(w => w.Label == label
                          && currentFrameIndex >= w.EntryFrame
                          && currentFrameIndex <= w.ExitFrame);
    if (containingWaypoint != null)
    {
        var entryBox = GetBboxesForFrame(containingWaypoint.EntryFrame)
            .FirstOrDefault(b => b.Label == label && !b.IsDeleted);
        if (entryBox != null) return GetBoxId(entryBox);
    }

    // entryFrameIndex 가 설정되어 있고 현 프레임이 Entry 이후이면 Entry 박스 ID 승계
    if (entryFrameIndex.HasValue && currentFrameIndex >= entryFrameIndex.Value)
    {
        var entryBox = GetBboxesForFrame(entryFrameIndex.Value)
            .FirstOrDefault(b => b.Label == label && !b.IsDeleted);
        if (entryBox != null) return GetBoxId(entryBox);
    }

    // 현재 프레임의 같은 Label selectedBox ID
    if (selectedBox != null && selectedBox.Label == label && selectedBox.FrameIndex == currentFrameIndex)
        return GetBoxId(selectedBox);

    // 사용자가 Ctrl+숫자로 지정한 currentAssignedId (2 이상이거나 명시 지정)
    if (currentAssignedId >= 1) return currentAssignedId;

    return 1;
}
```

3. BBOX 생성 지점에서 ID 할당 라인을 다음으로 교체 (line 1471-1473 또는 유사 패턴 위치):
```csharp
// NEW-01 (D-06): ID 승계 적용
int resolvedId = ResolveIdForNewBox(currentSelectedLabel, _videoService.CurrentFrameIndex);
// 기존:
//   PersonId = currentSelectedLabel == "person" ? currentAssignedId : 0,
//   VehicleId = currentSelectedLabel == "vehicle" ? currentAssignedId : 0,
//   EventId = currentSelectedLabel == "event" ? currentAssignedId : 0,
// 변경:
PersonId = currentSelectedLabel == "person" ? resolvedId : 0,
VehicleId = currentSelectedLabel == "vehicle" ? resolvedId : 0,
EventId = currentSelectedLabel == "event" ? resolvedId : 0,
```

4. Line 430-437 에 또 다른 BBOX 생성 패턴이 있다면 (grep 으로 확인 필수) 동일 교체 적용. 두 지점 이상 있으면 둘 다 수정.

5. `currentAssignedId = 1` 초기화 라인(line 561, 53) 은 유지 (영상 교체 시 리셋). 단, `currentAssignedId` 를 사용자가 명시적으로 지정하지 않은 기본값 상태에서도 ResolveIdForNewBox 가 Waypoint/Entry 에서 ID 를 찾을 수 있으므로 해결됨.
  </action>
  <verify>
    <automated>
      cd "C:/Users/ANNA/AOLTv1.0" && dotnet build ASLTv1.0.csproj -c Debug -v q 2>&1 | tail -20
      # ResolveIdForNewBox 메서드 존재:
      grep -c "ResolveIdForNewBox" Forms/MainForm.cs
      # 최소 2건 (정의 + 호출)
      # BBOX 생성 시 resolvedId 사용:
      grep -n "resolvedId" Forms/MainForm.cs
      # 최소 4건 (할당 + 3개 ID 라인)
      # 기존 currentAssignedId 직접 할당 패턴이 남아있는지 (있으면 누락 가능):
      grep -n "PersonId = currentSelectedLabel.*currentAssignedId" Forms/MainForm.cs
      # 0 이어야 함 (모두 resolvedId 로 전환)
    </automated>
  </verify>
  <acceptance_criteria>
    - `Forms/MainForm.cs` 에 `private int ResolveIdForNewBox` 메서드 정의 1건 존재.
    - ResolveIdForNewBox 본문에 `waypointMarkers.FirstOrDefault` 패턴 존재 (Waypoint 범위 검사).
    - ResolveIdForNewBox 본문에 `GetBboxesForFrame(.*EntryFrame)` 패턴 존재.
    - BBOX 생성 코드에 `resolvedId` 변수 사용 확인 (grep 4건 이상).
    - 기존 `PersonId = currentSelectedLabel == "person" ? currentAssignedId : 0` 패턴이 0건 (모두 resolvedId 로 교체).
    - `dotnet build` 성공.
  </acceptance_criteria>
  <done>
    UAT: BBOX 생성 → Ctrl+3 으로 ID=3 지정 → Entry 프레임 설정 → Exit 프레임으로 이동 → 새 BBOX 그리기 → 새 박스의 ID=3 자동 승계 확인 (기본값 1 로 회귀하지 않음).
  </done>
</task>

</tasks>

<verification>
- 빌드 성공 (경고·오류 0 증가)
- 수동 UAT 4종: (1) 개별 박스 ID 변경 (`[5].mp4`), (2) Ctrl+N 자동 매칭, (3) Event 불일치 경고 (`[1](3).mp4`), (4) Person NumericUpDown (`[4].png`), (5) BBOX 생성 시 ID 승계 (`[1, 4].mp4`)
- 회귀: Vehicle/Event ComboBox 동작 유지, Undo/Redo 동작 유지
</verification>

<success_criteria>
ROADMAP Phase 5.6 Success Criteria #2, #3, #4, #5 충족:
2. BBOX 생성 → 사후 ID 지정 → Entry 설정 후 Exit 프레임 BBOX 생성 시 Entry 지정 ID가 유지된다
3. 선택된 개별 BBOX의 ID만 변경되고, 같은 클래스·같은 ID의 다른 박스는 영향받지 않는다
4. Ctrl+N 입력 시 Exit 프레임 BBOX의 ID가 Entry 프레임 BBOX의 ID와 자동 일치된다
5. Entry-Exit ID 불일치 시 Person/Vehicle/Event 모두 경고 메시지가 표시된다
</success_criteria>

<output>
After completion, create `.planning/phases/05.6-결함수정/05.6-02-SUMMARY.md`
- 각 결함(NEW-01~07) Before/After 코드 비교
- 새 메서드 시그니처 (`ChangeBoxIdOnly`, `ResolveIdForNewBox`, `IsIdAssignmentKey`)
- Ctrl+N 구현 플로우차트 또는 의사코드
</output>
