---
phase: 05.6-결함수정
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Forms/MainForm.cs                 # lines 272-310 (LoadVideoWithSubtitle 한국어 메시지), 1302-1339 (Waypoint 일괄 삭제), Form Load (온보딩 팝업 트리거)
  - Services/VideoService.cs          # line 135 (InvalidOperationException 메시지 한국어화)
  - Services/SettingsService.cs       # 신규 파일 — 온보딩 플래그 저장/로드
  - Forms/OnboardingForm.cs           # 신규 파일 — 온보딩 가이드 UI
  - Forms/OnboardingForm.Designer.cs  # 신규 Designer
  - ASLTv1.0.csproj                   # (자동 업데이트 — 신규 .cs 파일은 SDK-style csproj 에서 자동 include)
autonomous: false                     # 2개 checkpoint:human-verify 포함 (DF-1-06, DF-1-07)
requirements:
  - DF-1-06
  - DF-1-07
  - DF-1-11
  - DF-1-14
  - DF-1-18
execution_note: "Wave 1. 모든 작업은 Forms/MainForm.cs 에 소프트 터치 또는 신규 파일 생성. 05.6-01, 05.6-02 와 동일 파일을 건드리므로 같은 세션에서 순차 실행 권장."

must_haves:
  truths:
    - "ROADMAP-SC-09: 다중 선택된 Waypoint 에 대해 일괄 삭제가 가능하다"
    - "ROADMAP-SC-10: 최초 실행 시 '동영상 선택 → BBOX 생성 → Entry/Exit 설정' 가이드 UI가 표시된다"
    - "ROADMAP-SC-11: 지원하지 않는 파일 확장자 선택 시 오류 메시지가 전부 한국어로 출력된다"
    - "DF-1-06 Tab 순환 동작: 이미 구현된 Tab 순환 선택 동작이 유지됨 (UAT 검증)"
    - "DF-1-07 Ctrl+Z 동작: 이미 구현된 단위 Undo 동작이 유지됨 (UAT 검증)"
  artifacts:
    - path: "Forms/OnboardingForm.cs"
      provides: "3단계 가이드 (동영상 선택 → BBOX 생성 → Entry/Exit 설정) + '다시 보지 않기' 체크박스"
      contains: "3단계|동영상 선택|BBOX 생성|Entry/Exit|다시 보지 않기"
    - path: "Services/SettingsService.cs"
      provides: "JSON 기반 앱 설정 저장 (OnboardingShown 플래그)"
      contains: "OnboardingShown|%LOCALAPPDATA%"
    - path: "Forms/MainForm.cs"
      provides: "Form Load 시 온보딩 팝업 트리거, Waypoint 일괄 삭제 루프, 영상 로드 오류 한국어화"
      contains: "ShowOnboarding|foreach.*SelectedItems"
    - path: "Services/VideoService.cs"
      provides: "InvalidOperationException 메시지 한국어화"
      contains: "비디오 파일을 열 수 없습니다"
  key_links:
    - from: "MainForm Form Load 이벤트"
      to: "OnboardingForm"
      via: "SettingsService.IsFirstRun 체크 후 ShowDialog"
      pattern: "new OnboardingForm"
    - from: "btnDeleteSelectedWaypoint_Click (MainForm.cs:1302)"
      to: "다중 선택 Waypoint 루프"
      via: "SelectedItems 전체를 순회하며 삭제"
      pattern: "foreach.*SelectedItems"
    - from: "VideoService.LoadVideoAsync (line 135)"
      to: "한국어 메시지"
      via: "InvalidOperationException 문자열 교체"
      pattern: "비디오 파일을 열 수 없습니다"
---

<objective>
사용성 결함 5종 해결. 이 플랜은 **체크포인트 2건 포함** (DF-1-06, DF-1-07 은 동작 이미 구현 — UAT 검증만 필요).

**해결할 결함 5종**:
1. **DF-1-06** (체크포인트): Tab BBOX 순환 선택 동작 UAT 검증 (문서화는 D-12 에 따라 Phase 6 이월).
2. **DF-1-07** (체크포인트): Ctrl+Z 단위 Undo 동작 UAT 검증.
3. **DF-1-11**: 최초 실행 시 온보딩 가이드 UI 부재 → 3단계 가이드 팝업 신규 구현 + "다시 보지 않기" 플래그.
4. **DF-1-14**: `"Failed to open video file."` 영어 메시지 혼용 → 한국어 통일.
5. **DF-1-18**: Waypoint 다중 선택 일괄 삭제 미지원 → SelectedItems 루프 순회로 일괄 처리.

Purpose: ISO/IEC 25023 사용성(Usability) 특성. QA 관찰 기반 체감 품질 결함.
Output: 신규 Forms/OnboardingForm, Services/SettingsService, MainForm/VideoService 의 점적 수정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.6-결함수정/05.6-CONTEXT.md
@.planning/DEFECTS-INBOX.md
@Forms/MainForm.cs
@Services/VideoService.cs
@Forms/MainForm.Designer.cs

<interfaces>
From Services/VideoService.cs (line 133-136 — 영어 메시지):
```csharp
if (!videoCapture.IsOpened())
{
    throw new InvalidOperationException("Failed to open video file.");  // ❌ DF-1-14
}
```

From Forms/MainForm.cs (line 303 — 영어 메시지 2차 경로):
```csharp
if (!_videoService.IsVideoLoaded)
{
    MessageBox.Show("Failed to open video file.", "Error", MessageBoxButtons.OK, MessageBoxIcon.Error);  // ❌ DF-1-14
    return;
}
```

From Forms/MainForm.cs (line 1302-1339 — 단일 Waypoint 만 처리하는 기존 핸들러):
```csharp
private void btnDeleteSelectedWaypoint_Click(object sender, EventArgs e)
{
    WaypointMarker waypoint = null;
    string waypointType = "";

    if (listViewPersonWaypoints.SelectedItems.Count > 0)
        { waypoint = listViewPersonWaypoints.SelectedItems[0].Tag as WaypointMarker; waypointType = "Person"; }   // ❌ [0] 만 참조 → DF-1-18
    else if (listViewVehicleWaypoints.SelectedItems.Count > 0)
        { waypoint = listViewVehicleWaypoints.SelectedItems[0].Tag as WaypointMarker; waypointType = "Vehicle"; }
    else if (listViewEventWaypoints.SelectedItems.Count > 0)
        { waypoint = listViewEventWaypoints.SelectedItems[0].Tag as WaypointMarker; waypointType = "Event"; }
    // ... 단일 삭제 로직
}
```

ListView multi-select 는 `SelectedItems` 컬렉션이 이미 다중 항목 지원 (listView.MultiSelect 속성이 default true).

From Forms/MainForm.cs (line 2415 — Tab 순환 기존 구현):
```csharp
if (e.KeyCode == Keys.Tab) { CycleSelection(e.Shift); e.Handled = true; return; }
```

From Forms/MainForm.cs (line 2452 — Undo 기존 구현):
```csharp
else if (e.Control && e.KeyCode == Keys.Z) { if (e.Shift) Redo(); else Undo(); e.Handled = true; }
```

LocalAppData 패턴 (Services/LogService.cs:29-33 참고):
```csharp
string logDir = Path.Combine(
    Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData),
    "ANNA", "ASLT", "logs");
```
→ SettingsService 도 같은 루트 `%LOCALAPPDATA%\ANNA\ASLT\settings.json` 사용.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 영상 로드 오류 메시지 한국어화 (DF-1-14)</name>
  <files>Services/VideoService.cs, Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-15)
    - .planning/DEFECTS-INBOX.md (DF-1-14)
    - Services/VideoService.cs lines 97-155 (LoadVideoAsync 전체)
    - Forms/MainForm.cs lines 272-365 (LoadVideoWithSubtitle + 예외 핸들러)
  </read_first>
  <behavior>
    - VideoService.LoadVideoAsync 의 `"Failed to open video file."` 메시지를 `"비디오 파일을 열 수 없습니다. 파일이 손상되었거나 지원하지 않는 형식일 수 있습니다."` 로 교체.
    - MainForm.LoadVideoWithSubtitle line 303 의 MessageBox 메시지도 동일하게 한국어로 교체 + 제목 `"오류"` 로 통일.
    - 기존 다른 catch 블록의 메시지는 이미 한국어 (line 340, 349, 358) → 유지.
    - 한국어 문구는 사용자 친화적 (원인 + 가능한 해결) 포함.
  </behavior>
  <action>
1. `Services/VideoService.cs:135` 의 다음 라인:
```csharp
throw new InvalidOperationException("Failed to open video file.");
```
을 다음으로 교체:
```csharp
throw new InvalidOperationException(
    "비디오 파일을 열 수 없습니다.\n\n" +
    "원인:\n" +
    "1. 파일이 손상되었거나 지원하지 않는 확장자입니다.\n" +
    "2. 코덱을 지원하지 않습니다.\n\n" +
    "해결 방법: MP4(H.264) 등 지원 형식의 파일인지 확인하세요.");
```

2. `Forms/MainForm.cs:303` 의 다음 블록:
```csharp
if (!_videoService.IsVideoLoaded)
{
    MessageBox.Show("Failed to open video file.", "Error", MessageBoxButtons.OK, MessageBoxIcon.Error);
    return;
}
```
을 다음으로 교체:
```csharp
if (!_videoService.IsVideoLoaded)
{
    MessageBox.Show(
        "비디오 파일을 열 수 없습니다.\n\n" +
        "원인: 파일이 손상되었거나 지원하지 않는 확장자일 수 있습니다.\n" +
        "해결 방법: MP4(H.264) 등 지원 형식의 파일인지 확인하세요.",
        "오류",
        MessageBoxButtons.OK,
        MessageBoxIcon.Error);
    return;
}
```

3. `Services/VideoService.cs` 전체에서 다른 영어 메시지 발견 시 한국어화 (grep `"[A-Z][a-z]+.*failed\|Failed\|Error\|Unable"` Services/VideoService.cs). 단, Debug.WriteLine / Log.* 는 영문 프리픽스 유지 가능 (개발자용).
  </action>
  <verify>
    <automated>
      cd "C:/Users/ANNA/AOLTv1.0" && dotnet build ASLTv1.0.csproj -c Debug -v q 2>&1 | tail -20
      # 영문 메시지 제거 확인:
      grep -c "Failed to open video file" Services/VideoService.cs
      # 0 이어야 함
      grep -c "Failed to open video file" Forms/MainForm.cs
      # 0 이어야 함
      # 한국어 메시지 존재:
      grep -n "비디오 파일을 열 수 없습니다" Services/VideoService.cs
      # 1건
      grep -n "비디오 파일을 열 수 없습니다" Forms/MainForm.cs
      # 1건 이상
    </automated>
  </verify>
  <acceptance_criteria>
    - `Services/VideoService.cs` 에서 `"Failed to open video file."` 문자열 0건.
    - `Services/VideoService.cs` 에서 `"비디오 파일을 열 수 없습니다"` 문자열 1건 이상.
    - `Forms/MainForm.cs` 에서 `"Failed to open video file."` 문자열 0건.
    - `Forms/MainForm.cs` 에서 `"비디오 파일을 열 수 없습니다"` 문자열 1건 이상.
    - MessageBox 제목이 `"Error"` → `"오류"` 로 한국어화됨 (line 303 위치 해당 블록).
    - `dotnet build` 성공.
  </acceptance_criteria>
  <done>
    UAT: 지원하지 않는 확장자 파일 (예: .txt 강제 선택 또는 손상 .mp4) 로드 시 영문 메시지 노출 0건. 한국어 MessageBox 만 표시.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Waypoint 다중 선택 일괄 삭제 (DF-1-18)</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-16)
    - .planning/DEFECTS-INBOX.md (DF-1-18)
    - Forms/MainForm.cs lines 1302-1339 (btnDeleteSelectedWaypoint_Click 원본)
    - Forms/MainForm.Designer.cs lines 465-506 (listViewPersonWaypoints 등 MultiSelect 속성 확인 — default true)
  </read_first>
  <behavior>
    - `btnDeleteSelectedWaypoint_Click` 핸들러가 P/V/E 각 ListView 의 **전체 SelectedItems** 를 순회하며 삭제.
    - 다중 항목 선택된 경우 확인 프롬프트 1회: `"선택된 {count}개의 Waypoint를 삭제하시겠습니까?"` (Yes/No).
    - 단일 항목만 선택된 경우 기존 메시지 유지 `"선택한 {waypointType} Waypoint를 삭제하시겠습니까?"`.
    - 여러 ListView 에 걸쳐 선택된 경우 (Person 2개 + Vehicle 1개 등) 전부 합산.
  </behavior>
  <action>
1. `Forms/MainForm.cs:1302` 의 `btnDeleteSelectedWaypoint_Click` 메서드 전체를 다음으로 교체:
```csharp
private void btnDeleteSelectedWaypoint_Click(object sender, EventArgs e)
{
    // DF-1-18 (D-16): 다중 선택 일괄 삭제
    var toDelete = new List<(WaypointMarker wp, string type)>();

    foreach (ListViewItem item in listViewPersonWaypoints.SelectedItems)
        if (item.Tag is WaypointMarker wp) toDelete.Add((wp, "Person"));
    foreach (ListViewItem item in listViewVehicleWaypoints.SelectedItems)
        if (item.Tag is WaypointMarker wp) toDelete.Add((wp, "Vehicle"));
    foreach (ListViewItem item in listViewEventWaypoints.SelectedItems)
        if (item.Tag is WaypointMarker wp) toDelete.Add((wp, "Event"));

    if (toDelete.Count == 0)
    {
        MessageBox.Show("삭제할 Waypoint를 선택해주세요.", "알림",
            MessageBoxButtons.OK, MessageBoxIcon.Information);
        return;
    }

    string prompt = toDelete.Count == 1
        ? $"선택한 {toDelete[0].type} Waypoint를 삭제하시겠습니까?"
        : $"선택된 {toDelete.Count}개의 Waypoint를 삭제하시겠습니까?";

    var result = MessageBox.Show(prompt, "Waypoint 삭제",
        MessageBoxButtons.YesNo, MessageBoxIcon.Warning);
    if (result != DialogResult.Yes) return;

    int deletedBoxCount = 0;
    foreach (var (waypoint, type) in toDelete)
    {
        var boxesToDelete = boundingBoxes.Where(b =>
            b.Label == waypoint.Label
            && GetBoxId(b) == waypoint.ObjectId
            && b.FrameIndex >= waypoint.EntryFrame
            && b.FrameIndex <= waypoint.ExitFrame).ToList();

        foreach (var box in boxesToDelete)
        {
            AddUndoAction(new UndoAction { Type = UndoActionType.RemoveBox, Box = CloneBoundingBox(box) });
            boundingBoxes.Remove(box);
            deletedBoxCount++;
        }

        if (selectedBox != null && boxesToDelete.Contains(selectedBox)) selectedBox = null;
        if (selectedWaypoint == waypoint) selectedWaypoint = null;
        waypointMarkers.Remove(waypoint);
    }

    UpdateWaypointListView();
    InvalidateBoxCache();
    UpdateBoxCount();
    UpdateBboxListDisplay();
    panelTimeline.Invalidate();
    pictureBoxVideo.Invalidate();
}
```

2. Designer 레벨에서 `listViewPersonWaypoints.MultiSelect = true;` 등이 명시적으로 설정되지 않아도 기본값이 true 이므로 변경 불필요. 단, 다중 선택 UX 를 위해 Designer 에서 `listViewPersonWaypoints.MultiSelect = true;` 를 명시하면 명확성 향상 — **선택적 수정**.
  </action>
  <verify>
    <automated>
      cd "C:/Users/ANNA/AOLTv1.0" && dotnet build ASLTv1.0.csproj -c Debug -v q 2>&1 | tail -20
      # foreach 루프 수 확인 (P/V/E × 1 = 3):
      grep -c "foreach (ListViewItem item in listView" Forms/MainForm.cs
      # 3 이상
      # 다중 선택 프롬프트 문구:
      grep -n "선택된.*개의 Waypoint를 삭제" Forms/MainForm.cs
      # 1건
      # 기존 단일 선택 로직이 [0] 참조하는지 (있으면 미교체):
      grep -n "SelectedItems\[0\]\.Tag as WaypointMarker" Forms/MainForm.cs
      # 0 이어야 함 (foreach 로 교체됨)
    </automated>
    <manual>
      UAT: 여러 Waypoint 를 Ctrl+클릭으로 다중 선택 → Delete 키 또는 삭제 버튼 → 프롬프트에 "N개의 Waypoint를" 표시 확인 → Yes → 모두 삭제.
    </manual>
  </verify>
  <acceptance_criteria>
    - `Forms/MainForm.cs` btnDeleteSelectedWaypoint_Click 메서드 본문에 `foreach (ListViewItem item in listView*.SelectedItems)` 패턴 3건 (P/V/E).
    - `Forms/MainForm.cs` 에 `"선택된 {toDelete.Count}개의 Waypoint를 삭제하시겠습니까?"` 또는 동등 string interpolation 문자열 존재.
    - 기존 `SelectedItems[0].Tag as WaypointMarker` 패턴 0건 (모두 foreach 로 교체).
    - 삭제 루프가 `waypointMarkers.Remove(waypoint)` 를 각 waypoint 마다 호출.
    - `dotnet build` 성공.
  </acceptance_criteria>
  <done>
    UAT 재현: Person 2개 + Vehicle 1개 Ctrl+클릭 선택 → Delete → 한 번의 프롬프트 → 3개 모두 제거.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 온보딩 가이드 UI 신규 구현 (DF-1-11)</name>
  <files>Services/SettingsService.cs, Forms/OnboardingForm.cs, Forms/OnboardingForm.Designer.cs, Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-14)
    - .planning/DEFECTS-INBOX.md (DF-1-11)
    - Services/LogService.cs (LocalAppData 경로 패턴 — `%LOCALAPPDATA%\ANNA\ASLT\`)
    - Forms/MainForm.cs (Form Load 이벤트 핸들러 또는 생성자 — 온보딩 호출 지점)
    - Theme/DarkTheme.cs (신규 Form 스타일링용)
  </read_first>
  <behavior>
    - 최초 실행 (SettingsService.OnboardingShown == false) 시 MainForm 이 Shown 이벤트에서 OnboardingForm 을 ShowDialog 로 표시.
    - OnboardingForm 3단계 구성:
      - Step 1: "동영상 선택" — 상단 메뉴 혹은 툴바에서 영상 파일을 여는 방법 설명
      - Step 2: "BBOX 생성" — 마우스 드래그로 객체 영역 지정, F1/F2/F3 라벨 전환 안내
      - Step 3: "Entry/Exit 설정" — Entry 버튼 또는 E 키로 시작 프레임 지정, Exit 또는 X 키로 종료 프레임 지정
    - "이전" / "다음" 버튼으로 단계 이동, 마지막 단계에서 "완료" 버튼.
    - 하단에 체크박스 "다시 보지 않기" — 체크 후 완료 시 SettingsService.OnboardingShown = true 저장.
    - 다크 테마 적용 (Theme/DarkTheme.cs 패턴).
    - 취소 버튼(ESC 또는 X) 으로 닫아도 설정은 저장되지 않음 (다음에 다시 표시).
  </behavior>
  <action>
1. `Services/SettingsService.cs` 신규 파일 생성:
```csharp
using System;
using System.IO;
using Newtonsoft.Json;
using Serilog;

namespace ASLTv1.Services
{
    /// <summary>
    /// DF-1-11 (D-14): 앱 설정 로컬 저장소 (%LOCALAPPDATA%\ANNA\ASLT\settings.json).
    /// 온보딩 표시 여부 등 사용자 선호를 영속화한다.
    /// </summary>
    public static class SettingsService
    {
        private static readonly string SettingsDir = Path.Combine(
            Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData),
            "ANNA", "ASLT");
        private static readonly string SettingsPath = Path.Combine(SettingsDir, "settings.json");

        public class AppSettings
        {
            public bool OnboardingShown { get; set; } = false;
        }

        private static AppSettings _cached;

        public static AppSettings Load()
        {
            if (_cached != null) return _cached;
            try
            {
                if (File.Exists(SettingsPath))
                {
                    string json = File.ReadAllText(SettingsPath);
                    _cached = JsonConvert.DeserializeObject<AppSettings>(json) ?? new AppSettings();
                }
                else _cached = new AppSettings();
            }
            catch (Exception ex)
            {
                Log.Warning(ex, "[Settings 로드 실패] 기본값 사용");
                _cached = new AppSettings();
            }
            return _cached;
        }

        public static void Save(AppSettings settings)
        {
            try
            {
                Directory.CreateDirectory(SettingsDir);
                string json = JsonConvert.SerializeObject(settings, Formatting.Indented);
                File.WriteAllText(SettingsPath, json);
                _cached = settings;
            }
            catch (Exception ex)
            {
                Log.Error(ex, "[Settings 저장 실패] {Path}", SettingsPath);
            }
        }

        public static bool IsFirstRun() => !Load().OnboardingShown;

        public static void MarkOnboardingShown()
        {
            var s = Load();
            s.OnboardingShown = true;
            Save(s);
        }
    }
}
```

2. `Forms/OnboardingForm.cs` 신규 파일 생성 (단순 3-step 카드 전환형):
```csharp
using System;
using System.Drawing;
using System.Windows.Forms;
using ASLTv1.Services;
using ASLTv1.Theme;

namespace ASLTv1.Forms
{
    /// <summary>
    /// DF-1-11 (D-14): 최초 실행 시 3단계 가이드 팝업.
    /// </summary>
    public partial class OnboardingForm : Form
    {
        private int _currentStep = 0;
        private readonly (string Title, string Body)[] _steps = new[]
        {
            ("1. 동영상 선택",
             "상단 툴바의 '영상 열기' 버튼을 누르거나\n메뉴에서 라벨링할 비디오 파일(MP4, AVI 등)을 선택하세요."),
            ("2. BBOX 생성",
             "F1(Person) / F2(Vehicle) / F3(Event) 로 클래스를 선택한 뒤\n비디오 화면에서 마우스를 드래그해 객체 영역을 지정하세요.\nCtrl+숫자 키로 객체 ID를 부여할 수 있습니다."),
            ("3. Entry/Exit 설정",
             "추적을 시작할 프레임에서 Entry 버튼(또는 E 키),\n끝나는 프레임에서 Exit 버튼(또는 X 키)을 눌러\nWaypoint 구간을 설정합니다.\n\nCtrl+S 로 COCO JSON 형식으로 저장할 수 있습니다.")
        };

        public OnboardingForm()
        {
            InitializeComponent();
            DarkTheme.ApplyToControls(this);   // 또는 기존 DarkTheme 진입점 메서드 이름 확인 후 호출
            RenderStep();
        }

        private void RenderStep()
        {
            labelTitle.Text = _steps[_currentStep].Title;
            labelBody.Text = _steps[_currentStep].Body;
            labelProgress.Text = $"{_currentStep + 1} / {_steps.Length}";
            btnPrev.Enabled = _currentStep > 0;
            btnNext.Text = _currentStep == _steps.Length - 1 ? "완료" : "다음";
        }

        private void btnPrev_Click(object sender, EventArgs e)
        {
            if (_currentStep > 0) { _currentStep--; RenderStep(); }
        }

        private void btnNext_Click(object sender, EventArgs e)
        {
            if (_currentStep < _steps.Length - 1) { _currentStep++; RenderStep(); return; }
            // 마지막 단계 완료
            if (chkDoNotShowAgain.Checked) SettingsService.MarkOnboardingShown();
            this.DialogResult = DialogResult.OK;
            this.Close();
        }
    }
}
```

3. `Forms/OnboardingForm.Designer.cs` 신규 파일 생성 (단순 WinForms 컨트롤 구성 — Label × 3, Button × 2, CheckBox × 1):
```csharp
namespace ASLTv1.Forms
{
    partial class OnboardingForm
    {
        private System.ComponentModel.IContainer components = null;
        private System.Windows.Forms.Label labelTitle;
        private System.Windows.Forms.Label labelBody;
        private System.Windows.Forms.Label labelProgress;
        private System.Windows.Forms.Button btnPrev;
        private System.Windows.Forms.Button btnNext;
        private System.Windows.Forms.CheckBox chkDoNotShowAgain;

        protected override void Dispose(bool disposing)
        {
            if (disposing && (components != null)) components.Dispose();
            base.Dispose(disposing);
        }

        private void InitializeComponent()
        {
            this.labelTitle = new System.Windows.Forms.Label();
            this.labelBody = new System.Windows.Forms.Label();
            this.labelProgress = new System.Windows.Forms.Label();
            this.btnPrev = new System.Windows.Forms.Button();
            this.btnNext = new System.Windows.Forms.Button();
            this.chkDoNotShowAgain = new System.Windows.Forms.CheckBox();
            this.SuspendLayout();

            // labelTitle
            this.labelTitle.Location = new System.Drawing.Point(20, 20);
            this.labelTitle.Size = new System.Drawing.Size(460, 40);
            this.labelTitle.Font = new System.Drawing.Font("Segoe UI", 16F, System.Drawing.FontStyle.Bold);
            this.labelTitle.Text = "";

            // labelBody
            this.labelBody.Location = new System.Drawing.Point(20, 70);
            this.labelBody.Size = new System.Drawing.Size(460, 180);
            this.labelBody.Font = new System.Drawing.Font("Segoe UI", 11F);
            this.labelBody.Text = "";

            // labelProgress
            this.labelProgress.Location = new System.Drawing.Point(20, 260);
            this.labelProgress.Size = new System.Drawing.Size(80, 22);
            this.labelProgress.Text = "1 / 3";

            // chkDoNotShowAgain
            this.chkDoNotShowAgain.Location = new System.Drawing.Point(20, 300);
            this.chkDoNotShowAgain.Size = new System.Drawing.Size(200, 24);
            this.chkDoNotShowAgain.Text = "다시 보지 않기";

            // btnPrev
            this.btnPrev.Location = new System.Drawing.Point(280, 300);
            this.btnPrev.Size = new System.Drawing.Size(90, 28);
            this.btnPrev.Text = "이전";
            this.btnPrev.Click += new System.EventHandler(this.btnPrev_Click);

            // btnNext
            this.btnNext.Location = new System.Drawing.Point(390, 300);
            this.btnNext.Size = new System.Drawing.Size(90, 28);
            this.btnNext.Text = "다음";
            this.btnNext.Click += new System.EventHandler(this.btnNext_Click);

            // OnboardingForm
            this.ClientSize = new System.Drawing.Size(500, 345);
            this.Controls.Add(this.labelTitle);
            this.Controls.Add(this.labelBody);
            this.Controls.Add(this.labelProgress);
            this.Controls.Add(this.chkDoNotShowAgain);
            this.Controls.Add(this.btnPrev);
            this.Controls.Add(this.btnNext);
            this.FormBorderStyle = System.Windows.Forms.FormBorderStyle.FixedDialog;
            this.StartPosition = System.Windows.Forms.FormStartPosition.CenterParent;
            this.MaximizeBox = false;
            this.MinimizeBox = false;
            this.Text = "AOLT 시작 가이드";
            this.ResumeLayout(false);
        }
    }
}
```

4. `Forms/MainForm.cs` Form Load 또는 Shown 이벤트 핸들러에서 온보딩 트리거 추가:
   - grep `MainForm_Load\|protected override void OnShown\|private void MainForm_Shown` 로 진입점 확인.
   - 없으면 다음 추가:
```csharp
protected override void OnShown(EventArgs e)
{
    base.OnShown(e);

    // DF-1-11 (D-14): 최초 실행 시 온보딩 가이드
    if (Services.SettingsService.IsFirstRun())
    {
        using (var onboard = new Forms.OnboardingForm())
        {
            onboard.ShowDialog(this);
        }
    }
}
```

5. `DarkTheme.ApplyToControls` 또는 해당 Theme 엔트리 메서드의 정확한 이름을 `Theme/DarkTheme.cs` 에서 grep 으로 확인하고 OnboardingForm 생성자에서 올바른 이름 호출. (기존 MainForm 에서 같은 호출 패턴 참조).
  </action>
  <verify>
    <automated>
      cd "C:/Users/ANNA/AOLTv1.0" && dotnet build ASLTv1.0.csproj -c Debug -v q 2>&1 | tail -20
      ls Services/SettingsService.cs Forms/OnboardingForm.cs Forms/OnboardingForm.Designer.cs
      # 3 파일 모두 존재
      grep -c "SettingsService" Forms/MainForm.cs
      # 최소 1건 (IsFirstRun 호출)
      grep -c "new Forms.OnboardingForm\|new OnboardingForm" Forms/MainForm.cs
      # 1건 (인스턴스화)
      grep -n "다시 보지 않기" Forms/OnboardingForm.Designer.cs
      # 1건
      grep -n "MarkOnboardingShown" Services/SettingsService.cs Forms/OnboardingForm.cs
      # 각각 1건
    </automated>
    <manual>
      UAT: 최초 실행 시 팝업 표시 → 다음/이전 → "다시 보지 않기" 체크 + "완료" → 앱 재시작 시 팝업 미표시 검증.
      재검증: %LOCALAPPDATA%\ANNA\ASLT\settings.json 열어서 OnboardingShown=true 확인.
    </manual>
  </verify>
  <acceptance_criteria>
    - `Services/SettingsService.cs` 파일 존재 + `public static bool IsFirstRun()` 메서드 정의.
    - `Services/SettingsService.cs` 에 `%LOCALAPPDATA%\ANNA\ASLT\settings.json` 또는 동등 `Environment.SpecialFolder.LocalApplicationData` + `"ANNA", "ASLT"` 경로 구성 존재.
    - `Forms/OnboardingForm.cs` 파일 존재 + 3-step 전환 로직 (`_currentStep`, `_steps`) 포함.
    - `Forms/OnboardingForm.Designer.cs` 파일 존재 + CheckBox "다시 보지 않기" 포함.
    - `Forms/MainForm.cs` 에 `SettingsService.IsFirstRun()` 호출 + `new OnboardingForm` 인스턴스화 + `ShowDialog` 호출.
    - SettingsService.Save 가 JSON 직렬화를 Newtonsoft.Json 사용 (`JsonConvert.SerializeObject`).
    - `dotnet build` 성공.
  </acceptance_criteria>
  <done>
    UAT: 최초 실행 시 팝업 자동 표시 + 3단계 내비게이션 동작 + "다시 보지 않기" 체크 시 재실행 미표시.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: DF-1-06 Tab 순환 선택 UAT 검증 (체크포인트)</name>
  <what-built>
    기존에 구현된 Tab 키 BBOX 순환 선택 동작 (`Forms/MainForm.cs:2415` `CycleSelection(e.Shift)`).
  </what-built>
  <how-to-verify>
    1. 앱 실행, 영상 로드.
    2. 한 프레임에 BBOX 3개 이상 생성 (F1, 드래그 반복).
    3. Tab 키 반복 입력: BBOX 1 → 2 → 3 → 1 (순환) 순서대로 selectedBox 이동 확인.
    4. Shift+Tab 입력: 역방향 순환 (3 → 2 → 1 → 3) 확인.
    5. 마지막 BBOX 에서 Tab → 첫 번째 BBOX 로 돌아가는지 확인 (= 순환).

    관찰 지표:
    - pictureBoxVideo 상단 selection 하이라이트가 매 Tab 입력마다 이동.
    - 사이드바의 선택된 BBOX 하이라이트가 이동.
    - 에러 메시지/크래시 없음.
  </how-to-verify>
  <resume-signal>
    사용자가 `"approved"` 또는 `"DF-1-06 확인 완료"` 로 응답 시 → 통과.
    `"FAIL: {설명}"` 으로 응답 시 → 별도 수정 플랜 생성 필요.
  </resume-signal>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-12 — 문서화는 Phase 6 이월, 본 Phase 는 UAT 만)
    - Forms/MainForm.cs line 2415 (Tab 핸들러)
    - Forms/MainForm.cs `CycleSelection` 메서드 (grep 으로 확인)
  </read_first>
  <acceptance_criteria>
    - 사용자가 `"approved"` 회신.
    - 또는 FAIL 응답이 올 경우, 새 gap 플랜으로 Phase 5.6 에 추가.
  </acceptance_criteria>
  <done>사용자 승인 시 완료.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: DF-1-07 Ctrl+Z 단위 Undo UAT 검증 (체크포인트)</name>
  <what-built>
    기존에 구현된 Ctrl+Z 단위 Undo 동작 (`Forms/MainForm.cs:2452`, `AddUndoAction` 단일 choke-point — Phase 04 결정 사항).
  </what-built>
  <how-to-verify>
    1. 앱 실행, 영상 로드.
    2. 순차 조작 5~10단계 수행:
       - BBOX 생성 (A)
       - 이동 (W/A/S/D)
       - 리사이즈
       - 다른 BBOX 생성 (B)
       - B 삭제
       - C 생성
    3. Ctrl+Z 반복 입력: 각 Z 누를 때마다 직전 조작 **한 단계씩만** 역순으로 되돌아감.
    4. Ctrl+Y (또는 Ctrl+Shift+Z) 로 Redo → 동일하게 한 단계씩 복원.

    관찰 지표:
    - 한 번의 Ctrl+Z 가 모든 조작을 초기화하지 않음 (DF-1-07 원래 증상).
    - 각 Undo 액션 단위가 명확 (생성 / 삭제 / 이동 / 리사이즈 분리).
    - 스택 상한 (MAX_UNDO_STACK) 초과 시 가장 오래된 항목 제거.
  </how-to-verify>
  <resume-signal>
    사용자가 `"approved"` 또는 `"DF-1-07 확인 완료"` 로 응답 시 → 통과.
  </resume-signal>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-13)
    - Forms/MainForm.cs line 2452 (Ctrl+Z 핸들러), `AddUndoAction` 호출부
    - .planning/phases/04-성능-사용성/ (Phase 4 Undo 단일 choke-point 결정 이력)
  </read_first>
  <acceptance_criteria>
    - 사용자가 `"approved"` 회신.
    - 또는 FAIL 응답 시 새 gap 플랜으로 Phase 5.6 에 추가.
  </acceptance_criteria>
  <done>사용자 승인 시 완료.</done>
</task>

</tasks>

<verification>
- 빌드 성공 (새 파일 2개 포함, 경고 0 증가)
- 수동 UAT 5종: (1) 영문→한국어 메시지, (2) 다중 Waypoint 일괄 삭제, (3) 온보딩 팝업 및 플래그, (4) Tab 순환 체크포인트, (5) Ctrl+Z 단위 Undo 체크포인트
- 회귀: settings.json 누락 시 기본값 동작, 기존 단일 Waypoint 삭제 UX 유지
</verification>

<success_criteria>
ROADMAP Phase 5.6 Success Criteria #9, #10, #11 충족:
9. 다중 선택된 Waypoint 에 대해 일괄 삭제가 가능하다
10. 최초 실행 시 "동영상 선택 → BBOX 생성 → Entry/Exit 설정" 가이드 UI가 표시된다
11. 지원하지 않는 파일 확장자 선택 시 오류 메시지가 전부 한국어로 출력된다
+ DF-1-06, DF-1-07 체크포인트 승인
</success_criteria>

<output>
After completion, create `.planning/phases/05.6-결함수정/05.6-04-SUMMARY.md`
- DF-1-06, 07 UAT 결과
- OnboardingForm 스크린샷 또는 레이아웃 설명
- settings.json 스키마
- 변경된 영어 → 한국어 메시지 목록
</output>
