---
phase: 05.6-결함수정
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Forms/MainForm.cs                 # lines 848-857 (btnEntry_Click/SetEntryMarker), 867-891 (btnExit_Click), 2454-2455 (Keys.E/Keys.X in MainForm_KeyDown)
autonomous: true
requirements:
  - DF-1-13
execution_note: "Wave 1. Touches Forms/MainForm.cs — execute sequentially with 05.6-02 and 05.6-04 in same session (avoid concurrent edits on same 3000-line file)."

must_haves:
  truths:
    - "ROADMAP-SC-01: 영상 미로드 상태에서 Entry/Exit 클릭 시 unhandled exception 없이 한국어 안내 메시지가 표시된다"
    - "영상 미로드 상태에서 E 키 / X 키 단축키를 눌러도 TimeSpan.FromSeconds(NaN) 예외가 발생하지 않는다"
    - "Entry 또는 Exit 조작이 차단될 때 사용자는 `영상을 먼저 로드해 주십시오.` 메시지를 본다"
  artifacts:
    - path: "Forms/MainForm.cs"
      provides: "btnEntry_Click / btnExit_Click / SetEntryMarker / SetExitMarkerAndCreateWaypoint / Keys.E / Keys.X 경로의 IsVideoLoaded 가드"
      contains: "!_videoService.IsVideoLoaded"
  key_links:
    - from: "btnEntry_Click (Forms/MainForm.cs:848)"
      to: "SetEntryMarker (Forms/MainForm.cs:859)"
      via: "진입부 IsVideoLoaded 가드 추가"
      pattern: "if \\(!_videoService\\.IsVideoLoaded\\)"
    - from: "btnExit_Click (Forms/MainForm.cs:867)"
      to: "SetExitMarkerAndCreateWaypoint (Forms/MainForm.cs:893)"
      via: "진입부 IsVideoLoaded 가드 추가"
      pattern: "if \\(!_videoService\\.IsVideoLoaded\\)"
    - from: "MainForm_KeyDown Keys.E/Keys.X branches (Forms/MainForm.cs:2454-2455)"
      to: "SetEntryMarker / SetExitMarkerAndCreateWaypoint"
      via: "호출 전 IsVideoLoaded 가드 (기존 라인 2413 체크 뒤에 있으므로 이미 보호됨 — 확인만)"
      pattern: "if \\(!_videoService\\.IsVideoLoaded\\) return"
---

<objective>
DF-1-13 크래시 차단. 영상이 로드되지 않은 상태에서 Entry / Exit 버튼(`btnEntry_Click`, `btnExit_Click`) 또는 단축키 `E` / `X` 입력 시 `TimeSpan.FromSeconds(currentFrameIndex / fps)` 호출에서 `fps = 0`으로 `Infinity / NaN`이 발생하여 unhandled exception으로 앱이 종료되는 크래시를 원천 차단한다.

Purpose: ISO/IEC 25023 신뢰성(Reliability) 특성 — Critical 결함 0건 충족. GS인증 1등급 필수.
Output: Forms/MainForm.cs 에 4개 지점(두 버튼 + 단축키 경로)에 `_videoService.IsVideoLoaded` 가드와 한국어 안내 메시지가 적용되어, 스크린샷 재현 시나리오(앱 실행 → 영상 로드 안 함 → Entry 클릭)에서 크래시 없이 MessageBox만 나타난다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.6-결함수정/05.6-CONTEXT.md
@.planning/DEFECTS-INBOX.md
@Forms/MainForm.cs
@Services/VideoService.cs

<interfaces>
From Services/VideoService.cs (line 66):
```csharp
public bool IsVideoLoaded => videoCapture != null && videoCapture.IsOpened();
```

From Forms/MainForm.cs (line 848-866 현재 상태 — 가드 없음):
```csharp
private void btnEntry_Click(object sender, EventArgs e)
{
    // USAB-09: Waypoint 선택 상태이면 해당 EntryFrame으로 이동, 아니면 기존 지정 동작
    if (selectedWaypoint != null)
    {
        LoadFrame(selectedWaypoint.EntryFrame);
        return;
    }
    SetEntryMarker();
}

private void SetEntryMarker()
{
    entryFrameIndex = _videoService.CurrentFrameIndex;
    TimeSpan entryTime = TimeSpan.FromSeconds(_videoService.CurrentFrameIndex / _videoService.Fps); // ← Fps==0 → NaN
    btnEntry.Text = $"Entry: {entryTime:hh\\:mm\\:ss}";
    panelTimeline.Invalidate();
}
```

From Forms/MainForm.cs (line 867-891 현재 상태 — try/catch는 있지만 진입부 가드 없음):
```csharp
private async void btnExit_Click(object sender, EventArgs e)
{
    if (selectedWaypoint != null) { LoadFrame(selectedWaypoint.ExitFrame); return; }
    try { await SetExitMarkerAndCreateWaypoint(); }
    catch (InvalidOperationException invEx) { ... }
    catch (Exception ex) { ... }
}
```

From Forms/MainForm.cs (line 2413, 2454-2455 — 단축키 경로):
```csharp
if (!_videoService.IsVideoLoaded) return;   // ← 이 early return 이후 E/X 브랜치가 옴 → 이미 보호됨 (검증만 필요)
...
else if (e.KeyCode == Keys.E && !e.Control && !e.Alt) { SetEntryMarker(); e.Handled = true; }
else if (e.KeyCode == Keys.X && !e.Control && !e.Alt) { _ = SetExitMarkerAndCreateWaypoint(); e.Handled = true; }
```

기존 선례 (05.5 Phase에서 RELI-06 가드 패턴):
- `_videoService.IsVideoLoaded` 체크 → 반환 전 한국어 MessageBox 표시
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Entry/Exit 버튼 및 SetEntryMarker 진입부 IsVideoLoaded 가드 추가 + Keys.E/Keys.X 단축키 경로 검증</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-11 항목 전체 — 한국어 메시지 원문 고정)
    - .planning/DEFECTS-INBOX.md (DF-1-13 재현 조건)
    - Forms/MainForm.cs lines 848-891 (btnEntry_Click, SetEntryMarker, btnExit_Click 원본)
    - Forms/MainForm.cs lines 2413, 2454-2455 (Keys.E/X 단축키 경로 — 이미 IsVideoLoaded early-return이 있는지 확인)
    - Services/VideoService.cs lines 43-66 (Fps / IsVideoLoaded 프로퍼티)
  </read_first>
  <behavior>
    - 영상 미로드 상태에서 Entry 버튼 클릭 → MessageBox "영상을 먼저 로드해 주십시오." (OK 버튼, Information 아이콘) 표시 후 즉시 return. TimeSpan 예외 없음.
    - 영상 미로드 상태에서 Exit 버튼 클릭 → 동일 MessageBox 표시 후 return.
    - 영상 미로드 상태에서 E 키 입력 → 기존 `if (!_videoService.IsVideoLoaded) return;` (line 2413)이 이미 먹히는지 확인. 먹지 않으면 E/X 브랜치 앞에 가드 추가.
    - 영상 로드 후 정상 동작은 변경 없음 (회귀 없음).
    - Waypoint 선택 상태에서 Entry/Exit 클릭 시 이동 동작(line 851, 870)은 유지 — 가드는 `selectedWaypoint != null` 분기 **이후**에 배치 (이동은 영상 미로드 상태에서도 의미 없으므로 가드 앞에 둬도 무방하지만, 기존 동작 보존을 위해 뒤에).
  </behavior>
  <action>
1. `Forms/MainForm.cs:848` `btnEntry_Click` 메서드를 다음과 같이 수정:
```csharp
private void btnEntry_Click(object sender, EventArgs e)
{
    // USAB-09: Waypoint 선택 상태이면 해당 EntryFrame으로 이동, 아니면 기존 지정 동작
    if (selectedWaypoint != null)
    {
        if (!_videoService.IsVideoLoaded)
        {
            MessageBox.Show("영상을 먼저 로드해 주십시오.", "알림", MessageBoxButtons.OK, MessageBoxIcon.Information);
            return;
        }
        LoadFrame(selectedWaypoint.EntryFrame);
        return;
    }

    // DF-1-13: 영상 미로드 상태에서 Entry 클릭 시 TimeSpan.FromSeconds(NaN) 크래시 차단
    if (!_videoService.IsVideoLoaded)
    {
        MessageBox.Show("영상을 먼저 로드해 주십시오.", "알림", MessageBoxButtons.OK, MessageBoxIcon.Information);
        return;
    }
    SetEntryMarker();
}
```

2. `Forms/MainForm.cs:859` `SetEntryMarker` 진입부에 방어적 가드 한 줄 추가 (단축키 경로 안전망):
```csharp
private void SetEntryMarker()
{
    // DF-1-13: 호출자 누락 대비 방어선
    if (!_videoService.IsVideoLoaded) return;

    entryFrameIndex = _videoService.CurrentFrameIndex;
    TimeSpan entryTime = TimeSpan.FromSeconds(_videoService.CurrentFrameIndex / _videoService.Fps);
    btnEntry.Text = $"Entry: {entryTime:hh\\:mm\\:ss}";
    panelTimeline.Invalidate();
}
```

3. `Forms/MainForm.cs:867` `btnExit_Click` 메서드를 다음과 같이 수정:
```csharp
private async void btnExit_Click(object sender, EventArgs e)
{
    if (selectedWaypoint != null)
    {
        if (!_videoService.IsVideoLoaded)
        {
            MessageBox.Show("영상을 먼저 로드해 주십시오.", "알림", MessageBoxButtons.OK, MessageBoxIcon.Information);
            return;
        }
        LoadFrame(selectedWaypoint.ExitFrame);
        return;
    }

    // DF-1-13: 영상 미로드 상태에서 Exit 클릭 시 fps==0 연산 차단
    if (!_videoService.IsVideoLoaded)
    {
        MessageBox.Show("영상을 먼저 로드해 주십시오.", "알림", MessageBoxButtons.OK, MessageBoxIcon.Information);
        return;
    }

    try { await SetExitMarkerAndCreateWaypoint(); }
    catch (InvalidOperationException invEx) { /* 기존 블록 유지 */
        Log.Error(invEx, "[Exit 설정 오류] {Message}", invEx.Message);
        MessageBox.Show($"Exit 마커 설정 실패:\n\n{invEx.Message}\n\n" +
            $"해결 방법: Entry 마커를 먼저 설정한 후 다시 시도하세요.",
            "오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
    }
    catch (Exception ex) {
        Log.Error(ex, "[Exit 버튼 오류] {Message}", ex.Message);
        MessageBox.Show($"Exit 설정 중 오류 발생:\n{ex.Message}", "오류", MessageBoxButtons.OK, MessageBoxIcon.Error);
    }
}
```

4. `Forms/MainForm.cs:893` `SetExitMarkerAndCreateWaypoint` 진입부에 방어적 가드 추가:
```csharp
private async Task SetExitMarkerAndCreateWaypoint()
{
    // DF-1-13: 호출자 누락 대비 방어선
    if (!_videoService.IsVideoLoaded) return;

    int currentFrameIndex = _videoService.CurrentFrameIndex;
    double fps = _videoService.Fps;
    // ... 기존 코드
}
```

5. `Forms/MainForm.cs` 단축키 경로 검증: `MainForm_KeyDown`의 line 2413 에 있는 `if (!_videoService.IsVideoLoaded) return;` 가 Keys.E/Keys.X 브랜치(line 2454-2455)보다 **앞에** 위치하는지 확인. 현재 구조상 이미 보호되므로 **추가 수정 불필요**. 단, 이 가드가 삭제되지 않도록 주석 추가:
```csharp
if (!_videoService.IsVideoLoaded) return;  // DF-1-13: 아래 Keys.E/X 등 영상 의존 브랜치 보호
```

6. 한국어 메시지 원문은 D-11 locked decision에 따라 정확히 `"영상을 먼저 로드해 주십시오."` 로 고정. 변형 금지.
  </action>
  <verify>
    <automated>
      cd "C:/Users/ANNA/AOLTv1.0" && dotnet build ASLTv1.0.csproj -c Debug -v q 2>&1 | tail -20
      # 빌드 성공 + 경고 0 증가
      # 이후 grep으로 가드 존재 확인:
      grep -n "영상을 먼저 로드해 주십시오" Forms/MainForm.cs
      # 최소 2건 (btnEntry_Click, btnExit_Click) 이상 매칭되어야 함
      grep -n "if (!_videoService.IsVideoLoaded)" Forms/MainForm.cs
      # btnEntry_Click, btnExit_Click, SetEntryMarker, SetExitMarkerAndCreateWaypoint, MainForm_KeyDown 포함 5건 이상
    </automated>
    <manual>
      수동 UAT: 앱 실행 → 영상 로드하지 않음 → (1) Entry 버튼 클릭 → MessageBox 확인 → (2) Exit 버튼 클릭 → MessageBox 확인 → (3) E 키 입력 → 반응 없음(또는 동일 메시지) → (4) X 키 입력 → 반응 없음 → (5) 앱 계속 동작.
    </manual>
  </verify>
  <acceptance_criteria>
    - `Forms/MainForm.cs` 에 `"영상을 먼저 로드해 주십시오."` 문자열이 최소 2회 등장 (grep으로 검증).
    - `Forms/MainForm.cs` `btnEntry_Click` 메서드 본문에 `if (!_videoService.IsVideoLoaded)` 패턴 존재.
    - `Forms/MainForm.cs` `btnExit_Click` 메서드 본문에 `if (!_videoService.IsVideoLoaded)` 패턴 존재.
    - `Forms/MainForm.cs` `SetEntryMarker` 메서드 본문에 `if (!_videoService.IsVideoLoaded)` 방어 가드 존재.
    - `Forms/MainForm.cs` `SetExitMarkerAndCreateWaypoint` 메서드 본문에 `if (!_videoService.IsVideoLoaded)` 방어 가드 존재.
    - `dotnet build ASLTv1.0.csproj -c Debug` 성공 (exit code 0).
    - 기존 Waypoint 선택 시 LoadFrame 분기(line 851, 870)가 유지되어야 함 — 이 패턴 문자열 `if (selectedWaypoint != null)` 가 btnEntry_Click / btnExit_Click 에 여전히 존재.
    - 한국어 안내 메시지가 `MessageBoxIcon.Information` 아이콘을 사용해야 함 (Warning/Error 아님).
  </acceptance_criteria>
  <done>
    빌드 성공. grep 기준 acceptance_criteria 6/6 통과. UAT 재현 시나리오(PDF p.7 스크린샷 기준)에서 TimeSpan 예외 0건.
  </done>
</task>

</tasks>

<verification>
- 빌드/정적 검증: `dotnet build` 경고·오류 0 증가
- 수동 UAT: 영상 미로드 상태에서 Entry/Exit 클릭 + E/X 키 → MessageBox만 표시, 앱 정상 동작
- 회귀 방지: 영상 로드 후 Entry/Exit 정상 동작 확인 (Waypoint 선택 + 미선택 모두)
</verification>

<success_criteria>
ROADMAP Phase 5.6 Success Criteria #1 충족:
"영상 미로드 상태에서 Entry/Exit 클릭 시 unhandled exception 없이 안내 메시지가 표시된다"
</success_criteria>

<output>
After completion, create `.planning/phases/05.6-결함수정/05.6-01-SUMMARY.md`
- DF-1-13 재현 시나리오 Before/After (예외 스택 vs. MessageBox)
- 수정된 라인 번호 목록
- 한국어 메시지 원문
</output>
