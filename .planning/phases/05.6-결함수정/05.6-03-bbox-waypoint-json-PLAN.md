---
phase: 05.6-결함수정
plan: 03
type: execute
wave: 3
depends_on:
  - 05.6-02
files_modified:
  - Forms/MainForm.cs                 # lines 76 (필드 추가), 255-267 (영상 전환 다이얼로그), ~440-460 (BBOX 삭제 → Waypoint 동반 삭제), ~605-615 (Export Empty JSON 가드), 2440-2450 (Delete key 핸들러)
  - Services/JsonService.cs           # ResolveJsonPath 활용, 파일 삭제 유틸
  - Services/VideoService.cs          # (필요 시) 영상 교체 전 CurrentVideoFile 조회
autonomous: true
requirements:
  - DF-1-03
  - DF-1-04
  - DF-1-05
  - DF-1-16
execution_note: "Wave 3 (after 05.6-02). Serial dependency chain on Forms/MainForm.cs. Plan 02 의 ChangeBoxIdOnly/ResolveIdForNewBox 를 재사용. Task 순서 중요: Task 1 (Waypoint 동반 삭제) → Task 2 (_autoSavedJsonPath 필드 + 롤백 삭제 + DeleteJsonForVideo 유틸 정의) → Task 3 (Empty JSON 프롬프트, Task 2 에서 선언된 필드/유틸 사용)."

must_haves:
  truths:
    - "ROADMAP-SC-06: Waypoint 구간 내 BBOX를 전부 삭제할 때 Waypoint 동반 삭제 여부를 묻는 프롬프트가 표시되고 선택 결과가 즉시 반영된다"
    - "ROADMAP-SC-07: Waypoint/BBOX 없는 상태로 JSON 저장 시도 시 'Waypoint가 없어 해당 JSON 파일은 삭제됩니다. 삭제하시겠습니까?' 프롬프트가 제공되고 선택이 파일 상태에 반영된다"
    - "ROADMAP-SC-08: 영상 전환 시 '저장하지 않은 편집이 있습니다…' 프롬프트에서 '아니요' 선택 시 자동 저장된 JSON이 롤백 삭제된다"
    - "DF-1-16 자동 해결 검증: 영상 전환 시 이전 영상 상태가 JSON 파일 레벨에서도 완전 초기화"
  artifacts:
    - path: "Forms/MainForm.cs"
      provides: "Waypoint 동반 삭제 프롬프트, Empty JSON 가드 교체, 영상 전환 롤백 삭제, _autoSavedJsonPath 추적 필드"
      contains: "_autoSavedJsonPath|Waypoint 구간 BBOX가 전부 삭제|해당 JSON 파일은 삭제"
    - path: "Services/JsonService.cs"
      provides: "JSON 파일 삭제 유틸 DeleteJsonForVideo + 감사 이벤트 기록 포함"
      contains: "DeleteJsonForVideo|ResolveJsonPath|AuditJsonDelete"
  key_links:
    - from: "BBOX Delete 경로 (MainForm.cs:2440-2446, 경로 전반)"
      to: "Waypoint 동반 삭제 프롬프트"
      via: "삭제 후 Waypoint 구간에 같은 Label+Id 박스가 0건이면 MessageBox → 예 시 waypointMarkers.Remove"
      pattern: "Waypoint 구간 BBOX가 전부 삭제됩니다. 해당 Waypoint도 함께 삭제"
    - from: "btnExportJson_Click (MainForm.cs:601-615)"
      to: "Empty JSON 삭제 프롬프트"
      via: "boundingBoxes.Count == 0 && waypointMarkers.Count == 0 시 'Waypoint가 없어 해당 JSON 파일은 삭제됩니다. 삭제하시겠습니까?' MessageBox"
      pattern: "해당 JSON 파일은 삭제됩니다"
    - from: "Video selection dialog (MainForm.cs:255-267)"
      to: "롤백 삭제 로직"
      via: "'아니요' 선택 시 _autoSavedJsonPath 존재하면 File.Delete"
      pattern: "_autoSavedJsonPath"
    - from: "SaveCurrentLabelingData (Waypoint 생성 시 자동 저장)"
      to: "_autoSavedJsonPath 상태 추적"
      via: "자동 저장 시 경로 기록; 새 영상 로드 시 리셋"
      pattern: "_autoSavedJsonPath = "
---

<objective>
DF-1-03 / DF-1-04 / DF-1-05 / DF-1-16 해결. BBOX-Waypoint-JSON 의 삼중 정합성 확보.

**해결할 결함 4종**:
1. **DF-1-03**: Waypoint 구간 내 마지막 BBOX 삭제 시 Waypoint 동반 삭제 프롬프트 부재 → 프롬프트 추가.
2. **DF-1-04**: Waypoint/BBOX 없는 상태에서 JSON 저장 시 Empty JSON 생성 또는 "저장할 라벨링 데이터가 없음" 메시지만 표시 → "해당 JSON 파일은 삭제됩니다" 프롬프트로 대체, 예 선택 시 파일 삭제.
3. **DF-1-05**: 영상 전환 시 "저장하지 않은 편집" 프롬프트에서 "아니요" 선택 시 자동 저장된 JSON 파일 잔존 → 롤백 삭제.
4. **DF-1-16**: DF-1-05 조치로 자동 해결 (증거: 영상 전환 시 이전 영상 JSON 상태가 완전 제거).

Purpose: ISO/IEC 25023 기능정확성(FUNC) 결함. QA팀이 영상 증거로 재현한 데이터 일관성 문제. COCO 포맷 정합성 요구에 필수.
Output: Forms/MainForm.cs + Services/JsonService.cs 에 Waypoint 동반 삭제, Empty JSON 가드, 롤백 삭제 플로우 구현.

**Task 순서 주의**: Task 1 → Task 2 (필드 + 유틸 선언) → Task 3 (Task 2 선언물 사용). 이 순서로 실행해야 빌드 에러 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.6-결함수정/05.6-CONTEXT.md
@.planning/DEFECTS-INBOX.md
@.planning/phases/05.6-결함수정/05.6-02-id-subsystem-PLAN.md
@Forms/MainForm.cs
@Services/JsonService.cs
@Services/VideoService.cs

<interfaces>
From Forms/MainForm.cs (line 255-267 — 영상 전환 다이얼로그 현재):
```csharp
if (_isDirty && _videoService != null && _videoService.IsVideoLoaded)
{
    var result = MessageBox.Show(
        "저장하지 않은 편집이 있습니다. 저장 후 전환하시겠습니까?",
        "저장 확인",
        MessageBoxButtons.YesNoCancel,
        MessageBoxIcon.Warning);
    if (result == DialogResult.Yes)
        SaveCurrentLabelingData();
    else if (result == DialogResult.Cancel)
        return;
    // ❌ DF-1-05: No(아니요) 선택 시 아무 동작 없음 → 자동 저장된 JSON 잔존
}
await LoadVideoWithSubtitle(ofd.FileName);
```

From Forms/MainForm.cs (line 601-615 — Export 현재):
```csharp
if (boundingBoxes.Count == 0)
{
    // ❌ DF-1-04: Waypoint 도 없는 상태에서 기존 JSON 파일이 있으면 삭제 프롬프트 필요
    MessageBox.Show("저장할 라벨링 데이터가 없습니다.", "경고", MessageBoxButtons.OK, MessageBoxIcon.Warning);
    return;
}
```

From Forms/MainForm.cs (line 2440-2451 — Delete key 핸들러):
```csharp
else if ((e.KeyCode == Keys.Delete || e.KeyCode == Keys.G) && selectedBox != null)
{
    AddUndoAction(new UndoAction { Type = UndoActionType.RemoveBox, Box = CloneBoundingBox(selectedBox) });
    boundingBoxes.Remove(selectedBox);
    selectedBox = null;
    InvalidateBoxCache(); UpdateBoxCount(); UpdateBboxListDisplay(); pictureBoxVideo.Invalidate(); e.Handled = true;
    // ❌ DF-1-03: Waypoint 구간 내 마지막 박스였는지 체크 없음
}
```

From Services/JsonService.cs (line 116):
```csharp
public string? ResolveJsonPath(string videoFilePath)   // 비디오 파일에 대응하는 JSON 경로 반환
```

From Forms/MainForm.cs 필드 (line 76):
```csharp
private bool _isDirty = false;
```

New field to add in Task 2 (Forms/MainForm.cs fields section, line ~77):
```csharp
/// <summary>DF-1-05 (D-09): Waypoint 생성 시 자동 저장된 JSON 경로. 영상 전환 시 '아니요' 선택하면 롤백 삭제용.</summary>
private string _autoSavedJsonPath = null;
```

From CONTEXT D-07 / D-08 / D-09 locked message strings:
- DF-1-03 프롬프트: `"Waypoint 구간 BBOX가 전부 삭제됩니다. 해당 Waypoint도 함께 삭제하시겠습니까?"` / 제목: `"Waypoint 삭제"` / Yes-No
- DF-1-04 프롬프트: `"Waypoint가 없어 해당 JSON 파일은 삭제됩니다. 삭제하시겠습니까?"` / 제목: `"JSON 파일 삭제"` / Yes-No
- DF-1-05: 기존 메시지 유지, No 브랜치에 롤백 삭제 로직만 추가
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: BBOX 삭제 시 Waypoint 동반 삭제 프롬프트 (DF-1-03)</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-07)
    - .planning/DEFECTS-INBOX.md (DF-1-03 — `결함3.DF-1-03_Waypoint 설정.mp4`)
    - Forms/MainForm.cs lines 2440-2451 (Delete key 핸들러)
    - Forms/MainForm.cs 전체에서 `boundingBoxes.Remove` 호출 지점 (grep `boundingBoxes\.Remove` — UI/Undo 경로 식별)
    - Forms/MainForm.cs lines 1302-1339 (btnDeleteSelectedWaypoint_Click — 참조용, 역방향 이미 있음)
    - .planning/phases/05.6-결함수정/05.6-02-id-subsystem-PLAN.md (Wave 2 완료 상태 확인)
  </read_first>
  <behavior>
    - 사용자가 BBOX 를 삭제 (Delete 키, G 키, 또는 UI 삭제 버튼) 할 때:
      1. 삭제 실행.
      2. 삭제된 박스가 Waypoint 구간(EntryFrame <= FrameIndex <= ExitFrame)에 속하고, 같은 Label+ObjectId 박스가 해당 Waypoint 구간 내에서 0건이 되면 프롬프트 표시.
      3. 프롬프트: `"Waypoint 구간 BBOX가 전부 삭제됩니다. 해당 Waypoint도 함께 삭제하시겠습니까?"` (Yes/No, Warning 아이콘)
      4. Yes: `waypointMarkers.Remove(waypoint)` + UI 업데이트 (리스트뷰, 타임라인, 박스 리스트).
      5. No: Waypoint 유지 (빈 Waypoint — 나중에 사용자가 다시 BBOX 를 추가할 수 있음).
    - 다중 박스 삭제(Waypoint 삭제 버튼 제외)에서도 동일 로직 적용 — 루프 돌고 나서 한 번만 체크.
  </behavior>
  <action>
1. `Forms/MainForm.cs` 에 헬퍼 메서드 신규 추가:
```csharp
/// <summary>
/// DF-1-03 (D-07): BBOX 삭제 후 해당 Waypoint 가 비어있는지 체크하고, 비어있으면 동반 삭제 프롬프트 표시.
/// 여러 박스를 한 번에 삭제한 경우에도 한 번만 호출 (중복 프롬프트 방지).
/// </summary>
/// <param name="deletedBox">방금 삭제된 박스 (Waypoint 매칭 기준)</param>
private void PromptWaypointDeletionIfEmpty(BoundingBox deletedBox)
{
    if (deletedBox == null) return;

    // 삭제된 박스가 속했던 Waypoint 탐색
    var waypoint = waypointMarkers.FirstOrDefault(w =>
        w.Label == deletedBox.Label
        && w.ObjectId == GetBoxId(deletedBox)
        && deletedBox.FrameIndex >= w.EntryFrame
        && deletedBox.FrameIndex <= w.ExitFrame);

    if (waypoint == null) return;

    // Waypoint 구간 내 같은 Label+ObjectId 박스가 아직 남아있는지
    bool hasRemaining = boundingBoxes.Any(b =>
        !b.IsDeleted
        && b.Label == waypoint.Label
        && GetBoxId(b) == waypoint.ObjectId
        && b.FrameIndex >= waypoint.EntryFrame
        && b.FrameIndex <= waypoint.ExitFrame);

    if (hasRemaining) return;

    var result = MessageBox.Show(
        "Waypoint 구간 BBOX가 전부 삭제됩니다. 해당 Waypoint도 함께 삭제하시겠습니까?",
        "Waypoint 삭제",
        MessageBoxButtons.YesNo,
        MessageBoxIcon.Warning);

    if (result == DialogResult.Yes)
    {
        waypointMarkers.Remove(waypoint);
        if (selectedWaypoint == waypoint) selectedWaypoint = null;
        UpdateWaypointListView();
        panelTimeline.Invalidate();
    }
    // No 선택 시 Waypoint 유지 (빈 Waypoint)
}
```

2. `Forms/MainForm.cs:2440` Delete 키 핸들러를 다음으로 교체:
```csharp
else if ((e.KeyCode == Keys.Delete || e.KeyCode == Keys.G) && selectedBox != null)
{
    var deletedSnapshot = CloneBoundingBox(selectedBox);  // 프롬프트용 스냅샷
    AddUndoAction(new UndoAction { Type = UndoActionType.RemoveBox, Box = deletedSnapshot });
    boundingBoxes.Remove(selectedBox);
    selectedBox = null;
    InvalidateBoxCache(); UpdateBoxCount(); UpdateBboxListDisplay(); pictureBoxVideo.Invalidate();

    // DF-1-03: Waypoint 구간 내 마지막 박스였는지 확인
    PromptWaypointDeletionIfEmpty(deletedSnapshot);

    e.Handled = true;
}
```

3. 기타 `boundingBoxes.Remove(...)` 호출 지점을 모두 grep 으로 확인 (선택 영역 삭제, 우클릭 삭제 등):
   - grep `boundingBoxes\.Remove` Forms/MainForm.cs
   - 각 호출 지점에 `PromptWaypointDeletionIfEmpty(<deletedBoxSnapshot>)` 호출 추가 (단, Undo/Redo 재실행 경로는 제외 — 거기서 프롬프트 재호출하면 사용자 혼란).
   - Undo/Redo 경로 식별: `Undo()`, `Redo()` 메서드 내부에서 호출되는 Remove 는 제외.

4. `btnDeleteSelectedWaypoint_Click` (line 1302) 은 **Waypoint 자체를 삭제하는 경로**이므로 PromptWaypointDeletionIfEmpty 호출 불필요 (이미 Waypoint 삭제 중).
  </action>
  <verify>
    <automated>
      cd "C:/Users/ANNA/AOLTv1.0" && dotnet build ASLTv1.0.csproj -c Debug -v q 2>&1 | tail -20
      grep -c "PromptWaypointDeletionIfEmpty" Forms/MainForm.cs
      # 최소 2건 (정의 + 호출)
      grep -n "Waypoint 구간 BBOX가 전부 삭제" Forms/MainForm.cs
      # 최소 1건
    </automated>
  </verify>
  <acceptance_criteria>
    - `Forms/MainForm.cs` 에 `PromptWaypointDeletionIfEmpty` 메서드 정의 1건 존재.
    - 메서드 본문에 `waypointMarkers.FirstOrDefault` + `boundingBoxes.Any` 패턴 포함.
    - 메서드 본문에 `"Waypoint 구간 BBOX가 전부 삭제됩니다. 해당 Waypoint도 함께 삭제하시겠습니까?"` 정확한 원문 포함.
    - MessageBoxButtons.YesNo + MessageBoxIcon.Warning 조합 사용.
    - Delete/G 키 핸들러에서 `PromptWaypointDeletionIfEmpty` 호출 (호출 전 `CloneBoundingBox` 로 스냅샷 확보).
    - `btnDeleteSelectedWaypoint_Click` (line 1302) 내부에서는 호출 없음 (자체가 Waypoint 삭제).
    - `dotnet build` 성공.
  </acceptance_criteria>
  <done>
    UAT: Waypoint 생성 후 그 구간 내 모든 BBOX 를 삭제 → 마지막 삭제 시 프롬프트 표시 → Yes 선택 시 Waypoint 리스트에서 제거 + 타임라인 마커 제거 확인.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: _autoSavedJsonPath 필드 + JsonService.DeleteJsonForVideo 유틸 + 영상 전환 롤백 삭제 (DF-1-05, DF-1-16)</name>
  <files>Forms/MainForm.cs, Services/JsonService.cs</files>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-09, D-10)
    - .planning/DEFECTS-INBOX.md (DF-1-05, DF-1-16 — `결함5.DF-1-05(JSON 저장).png`)
    - Forms/MainForm.cs lines 245-310 (영상 선택 다이얼로그 + 저장 확인 + LoadVideoWithSubtitle)
    - Forms/MainForm.cs 에서 SaveCurrentLabelingData 호출 지점 (grep — Waypoint 생성 시 자동 저장 경로 확인)
    - Forms/MainForm.cs lines 76-80 (private 필드 영역 — _autoSavedJsonPath 추가할 위치)
    - Services/JsonService.cs lines 116-140 (ResolveJsonPath 구현)
    - Services/JsonService.cs 전체 (JSON 삭제 메서드 존재 여부 확인)
    - Services/LogService.cs (Log.Information 사용 패턴)
  </read_first>
  <behavior>
    - **Task 2 는 Task 3 의 선행 조건.** 여기서 `_autoSavedJsonPath` 필드, `JsonService.DeleteJsonForVideo` 메서드를 모두 **신규 도입**하고, 영상 전환 다이얼로그의 "아니요" 분기 롤백 삭제 로직까지 구현.
    - `Forms/MainForm.cs` 필드에 `private string _autoSavedJsonPath = null;` 추가.
    - `SaveCurrentLabelingData` 가 Waypoint 생성 등에서 자동 호출되어 JSON 이 저장될 때마다 `_autoSavedJsonPath = _jsonService.ResolveJsonPath(_videoService.CurrentVideoFile)` 설정.
    - 영상 전환 다이얼로그에서 "아니요(No)" 선택 시:
      1. `_autoSavedJsonPath` 가 유효하고 파일 존재하면 `File.Delete` → 로그 `[AUDIT] 영상 전환 시 미저장 JSON 롤백 삭제`.
      2. `_autoSavedJsonPath = null` 리셋.
      3. 영상 교체 진행.
    - 새 영상 로드 성공 시 `_autoSavedJsonPath = null` 로 리셋.
    - `JsonService.DeleteJsonForVideo` 유틸은 Task 3 에서 Empty JSON 프롬프트 YES 분기에서도 사용됨 — 따라서 **여기서 먼저 구현**.
    - DeleteJsonForVideo 내부에 `Log.Information("[AUDIT] JSON 파일 삭제: {Path}", jsonPath);` 추가 (D-17a 감사 이벤트 선반영 — Plan 05 Task 2 에서 `LogService.AuditJsonDelete` 로 교체될 수 있으나 현재는 `Log.Information` 로 직접 기록하여 forward dependency 방지).
  </behavior>
  <action>
1. **Services/JsonService.cs** 에 JSON 삭제 유틸 메서드 추가. 파일 상단 using 블록에 `using Serilog;` 가 이미 있으면 재사용, 없으면 추가. `ResolveJsonPath` 메서드 근처 (line 116 이후) 에 다음 추가:
```csharp
/// <summary>
/// DF-1-04 (D-08): 비디오에 대응하는 COCO JSON 파일을 삭제한다.
/// 파일이 없으면 false, 삭제 성공하면 true.
/// 감사 이벤트 기록 포함.
/// </summary>
public bool DeleteJsonForVideo(string videoFilePath)
{
    string? jsonPath = ResolveJsonPath(videoFilePath);
    if (string.IsNullOrEmpty(jsonPath) || !File.Exists(jsonPath)) return false;
    try
    {
        File.Delete(jsonPath);
        Log.Information("[AUDIT] JSON 파일 삭제: {Path}", jsonPath);
        return true;
    }
    catch (Exception ex)
    {
        Log.Error(ex, "[JSON 삭제 오류] {Path}", jsonPath);
        throw;
    }
}
```

2. **Forms/MainForm.cs:76** 의 `private bool _isDirty = false;` 아래에 필드 추가:
```csharp
/// <summary>DF-1-05 (D-09): Waypoint 생성 시 자동 저장된 JSON 경로. 영상 전환 시 '아니요' 선택하면 롤백 삭제.</summary>
private string _autoSavedJsonPath = null;
```

3. `SaveCurrentLabelingData` 메서드를 find 하여 (grep `private.*SaveCurrentLabelingData` Forms/MainForm.cs), 저장 성공 경로 끝에 다음 추가:
```csharp
// DF-1-05: 자동 저장 경로 기록 (명시적 Ctrl+S 와 Waypoint-트리거 자동 저장 모두 기록)
_autoSavedJsonPath = _jsonService.ResolveJsonPath(_videoService.CurrentVideoFile);
```

4. **Forms/MainForm.cs:255-267** 영상 전환 다이얼로그 블록을 다음으로 교체:
```csharp
if (_isDirty && _videoService != null && _videoService.IsVideoLoaded)
{
    var result = MessageBox.Show(
        "저장하지 않은 편집이 있습니다. 저장 후 전환하시겠습니까?",
        "저장 확인",
        MessageBoxButtons.YesNoCancel,
        MessageBoxIcon.Warning);

    if (result == DialogResult.Cancel)
    {
        return;
    }
    else if (result == DialogResult.Yes)
    {
        SaveCurrentLabelingData();
    }
    else // DialogResult.No
    {
        // DF-1-05 (D-09): 자동 저장된 JSON 이 있으면 롤백 삭제
        if (!string.IsNullOrEmpty(_autoSavedJsonPath) && File.Exists(_autoSavedJsonPath))
        {
            try
            {
                File.Delete(_autoSavedJsonPath);
                Log.Information("[AUDIT] 영상 전환 시 미저장 JSON 롤백 삭제: {Path}", _autoSavedJsonPath);
            }
            catch (Exception ex)
            {
                Log.Error(ex, "[JSON 롤백 삭제 오류] {Path}", _autoSavedJsonPath);
            }
        }
        _autoSavedJsonPath = null;
    }
}
await LoadVideoWithSubtitle(ofd.FileName);
```

5. `LoadVideoWithSubtitle` 성공 경로 끝 (line 308 `_isDirty = false;` 근처) 에 다음 추가:
```csharp
_autoSavedJsonPath = null;   // DF-1-05: 새 영상 로드 시 이전 영상 자동 저장 경로 리셋
```

6. `LoadVideoAsync` 실패 catch 에서도 `_autoSavedJsonPath = null` 리셋 (안전).

7. 앱 종료 핸들러(line 2932 근처) 에서도 `_isDirty` 와 유사 처리 필요 여부 검토:
   - 사용자가 종료 시 "저장하지 않은 편집" → "아니요" 선택하면 동일 롤백 삭제 적용 (DF-1-05 와 동일 UX).
   - 만약 FormClosing 핸들러에 "아니요" 분기가 있으면 거기도 동일 로직 적용.
  </action>
  <verify>
    <automated>
      cd "C:/Users/ANNA/AOLTv1.0" && dotnet build ASLTv1.0.csproj -c Debug -v q 2>&1 | tail -20
      grep -c "_autoSavedJsonPath" Forms/MainForm.cs
      # 최소 5건 (선언 + 할당 3+ + File.Delete 분기 + 리셋)
      grep -n "영상 전환 시 미저장 JSON 롤백 삭제" Forms/MainForm.cs
      # 1건 (로그 메시지)
      grep -n "DialogResult\.No" Forms/MainForm.cs
      # 최소 1건 (명시적 No 분기)
      grep -n "DeleteJsonForVideo" Services/JsonService.cs
      # 1건 (정의)
      grep -n "\[AUDIT\] JSON 파일 삭제" Services/JsonService.cs
      # 1건 (로그 포함)
    </automated>
  </verify>
  <acceptance_criteria>
    - `Forms/MainForm.cs` 에 `private string _autoSavedJsonPath` 필드 선언 존재.
    - `SaveCurrentLabelingData` 성공 경로에서 `_autoSavedJsonPath = _jsonService.ResolveJsonPath(_videoService.CurrentVideoFile)` 패턴 존재.
    - 영상 전환 다이얼로그의 `DialogResult.No` 분기에서 `File.Delete(_autoSavedJsonPath)` 호출 포함.
    - 롤백 삭제는 try/catch 로 감싸 `Log.Error` 로 실패 처리.
    - `LoadVideoWithSubtitle` 성공 경로에서 `_autoSavedJsonPath = null` 리셋.
    - 기존 `_isDirty = false;` 처리는 유지.
    - `Services/JsonService.cs` 에 `public bool DeleteJsonForVideo(string videoFilePath)` 메서드 정의 존재.
    - `DeleteJsonForVideo` 본문에 `File.Delete(jsonPath)` 와 `Log.Information("[AUDIT] JSON 파일 삭제: ...")` 모두 포함.
    - `dotnet build` 성공.
  </acceptance_criteria>
  <done>
    UAT: 영상 A 로드 → BBOX + Waypoint 생성 (자동 저장 발생) → 영상 B 선택 → "아니요" 선택 → 영상 A 의 JSON 파일이 디스크에서 삭제되었는지 확인 (Explorer 로 검증). DF-1-16 자동 해결도 동시 검증 (이전 영상 상태 잔존 없음). 빌드 성공.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Empty JSON 방지 + 삭제 프롬프트 (DF-1-04)</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/05.6-결함수정/05.6-CONTEXT.md (D-08)
    - .planning/DEFECTS-INBOX.md (DF-1-04 — `결함4.DF-1-04 JSON 저장.mp4`)
    - Forms/MainForm.cs lines 601-615 (btnExportJson_Click 진입부)
    - (Task 2 완료 후) Forms/MainForm.cs 에 _autoSavedJsonPath 필드 존재, Services/JsonService.cs 에 DeleteJsonForVideo 존재 — 본 Task 는 이들을 **사용** (선언하지 않음)
  </read_first>
  <behavior>
    - `btnExportJson_Click` 진입부에서:
      1. `boundingBoxes.Count == 0 && waypointMarkers.Count == 0` 체크.
      2. 조건 충족 시 기존 JSON 파일 존재 여부 확인 (JsonService.ResolveJsonPath + File.Exists).
         - 파일 존재: `"Waypoint가 없어 해당 JSON 파일은 삭제됩니다. 삭제하시겠습니까?"` 프롬프트 (Yes/No, Warning).
           - Yes: `_jsonService.DeleteJsonForVideo(...)` 호출 + MessageBox "JSON 파일이 삭제되었습니다." + `_isDirty = false` + `_autoSavedJsonPath = null`.
           - No: 저장 취소 return.
         - 파일 미존재: `"저장할 라벨링 데이터가 없어 JSON 파일은 생성되지 않습니다."` (OK, Information) + return.
    - 기존 "저장할 라벨링 데이터가 없습니다." 단일 메시지 대체.
    - **전제**: Task 2 에서 `_autoSavedJsonPath` 필드와 `DeleteJsonForVideo` 메서드가 이미 정의되어 있어야 본 Task 빌드 성공.
  </behavior>
  <action>
1. `Forms/MainForm.cs:601` `btnExportJson_Click` 진입부를 다음으로 교체:
```csharp
private async void btnExportJson_Click(object sender, EventArgs e)
{
    try
    {
        if (string.IsNullOrEmpty(_videoService.CurrentVideoFile))
        {
            MessageBox.Show("먼저 비디오 파일을 로드해주세요.", "경고", MessageBoxButtons.OK, MessageBoxIcon.Warning);
            return;
        }

        // DF-1-04 (D-08): Waypoint/BBOX 없는 상태에서의 저장 시도 처리
        if (boundingBoxes.Count == 0 && waypointMarkers.Count == 0)
        {
            string? existingJson = _jsonService.ResolveJsonPath(_videoService.CurrentVideoFile);
            bool fileExists = !string.IsNullOrEmpty(existingJson) && File.Exists(existingJson);

            if (fileExists)
            {
                var decision = MessageBox.Show(
                    "Waypoint가 없어 해당 JSON 파일은 삭제됩니다. 삭제하시겠습니까?",
                    "JSON 파일 삭제",
                    MessageBoxButtons.YesNo,
                    MessageBoxIcon.Warning);
                if (decision == DialogResult.Yes)
                {
                    try
                    {
                        _jsonService.DeleteJsonForVideo(_videoService.CurrentVideoFile);
                        _isDirty = false;
                        _autoSavedJsonPath = null;   // Task 2 에서 선언한 필드
                        MessageBox.Show("JSON 파일이 삭제되었습니다.", "알림",
                            MessageBoxButtons.OK, MessageBoxIcon.Information);
                    }
                    catch (Exception delEx)
                    {
                        MessageBox.Show($"JSON 파일 삭제 실패:\n{delEx.Message}", "오류",
                            MessageBoxButtons.OK, MessageBoxIcon.Error);
                    }
                }
                return;
            }
            else
            {
                MessageBox.Show("저장할 라벨링 데이터가 없어 JSON 파일은 생성되지 않습니다.",
                    "알림", MessageBoxButtons.OK, MessageBoxIcon.Information);
                return;
            }
        }

        // 이하 기존 저장 플로우 (line 617부터) — 그대로 유지
        Form loadingForm = new Form { /* 기존 */ };
        // ...
    }
    catch (Exception ex) { /* 기존 */ }
}
```

2. 기존 line 613 의 `"저장할 라벨링 데이터가 없습니다."` 메시지 블록은 **제거** (위 로직으로 완전 대체).
  </action>
  <verify>
    <automated>
      cd "C:/Users/ANNA/AOLTv1.0" && dotnet build ASLTv1.0.csproj -c Debug -v q 2>&1 | tail -20
      # Task 3 고유 검증:
      grep -n "DeleteJsonForVideo" Forms/MainForm.cs
      # 최소 1건 (호출)
      grep -n "Waypoint가 없어 해당 JSON 파일은 삭제" Forms/MainForm.cs
      # 1건 (프롬프트)
      grep -c "저장할 라벨링 데이터가 없습니다" Forms/MainForm.cs
      # 이전 단일 메시지는 제거되었으므로 0 또는 1 (새 메시지로 대체)
      grep -c "boundingBoxes\.Count == 0 && waypointMarkers\.Count == 0" Forms/MainForm.cs
      # 1건
    </automated>
  </verify>
  <acceptance_criteria>
    - `Forms/MainForm.cs` btnExportJson_Click 에 `boundingBoxes.Count == 0 && waypointMarkers.Count == 0` 조건 존재.
    - `Forms/MainForm.cs` 에 `"Waypoint가 없어 해당 JSON 파일은 삭제됩니다. 삭제하시겠습니까?"` 정확한 원문 포함.
    - `Forms/MainForm.cs` 에서 `_jsonService.DeleteJsonForVideo(_videoService.CurrentVideoFile)` 호출 포함.
    - MessageBoxButtons.YesNo + MessageBoxIcon.Warning 조합 (DeleteJson 프롬프트).
    - `_autoSavedJsonPath = null` 리셋 라인이 YES 분기에 포함.
    - `dotnet build` 성공 (Task 2 가 이미 선언물 제공).
  </acceptance_criteria>
  <done>
    UAT: (A) Waypoint/BBOX 0건 상태에서 Ctrl+S → 기존 JSON 있으면 삭제 프롬프트, 없으면 알림 메시지. (B) Yes 선택 시 파일 삭제 확인.
  </done>
</task>

</tasks>

<verification>
- 빌드 성공 (경고·오류 0 증가)
- 수동 UAT 3종: (1) Waypoint 구간 마지막 BBOX 삭제 → 프롬프트 → Yes/No 각각, (2) 영상 전환 → No 시 이전 JSON 삭제, (3) Empty JSON 저장 시도 → 프롬프트 → Yes 시 파일 삭제
- 회귀: 기존 Ctrl+S 저장 정상, 기존 Waypoint 삭제 (btnDeleteSelectedWaypoint_Click) 정상
</verification>

<success_criteria>
ROADMAP Phase 5.6 Success Criteria #6, #7, #8 충족:
6. Waypoint 구간 내 BBOX를 전부 삭제할 때 Waypoint 동반 삭제 여부를 묻는 프롬프트가 표시되고 선택 결과가 즉시 반영된다
7. Waypoint/BBOX 없는 상태로 JSON 저장 시도 시 "Waypoint가 없어 해당 JSON 파일은 삭제됩니다. 삭제하시겠습니까?" 프롬프트가 제공되고 선택이 파일 상태에 반영된다
8. 영상 전환 시 "저장하지 않은 편집이 있습니다…" 프롬프트에서 "아니요" 선택 시 자동 저장된 JSON이 롤백 삭제된다
+ DF-1-16 자동 해결 검증
</success_criteria>

<output>
After completion, create `.planning/phases/05.6-결함수정/05.6-03-SUMMARY.md`
- DF-1-03, 04, 05, 16 Before/After 플로우 다이어그램
- 신규 필드 `_autoSavedJsonPath` 라이프사이클
- JsonService.DeleteJsonForVideo 시그니처 + 감사 로그 라인
</output>
