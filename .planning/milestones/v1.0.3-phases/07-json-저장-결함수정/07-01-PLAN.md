---
phase: 07-json-저장-결함수정
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Forms/MainForm.cs
autonomous: true
requirements:
  - FUNC-11
tags:
  - winforms
  - ui-state
  - defect-fix
must_haves:
  truths:
    - "메뉴 → JSON 저장 후 자동 reload 시 좌측 모드 버튼이 '전체선택' 활성 시각으로 정확히 표시된다 (편집모드 잔존 표시 0건)"
    - "MainForm 폼 최초 로드 시 좌측 모드 버튼 시각이 currentMode 기본값(Select)과 일치한다"
    - "btnSelectAll/btnEdit 클릭과 단축키 D1/D2 로 모드 전환 시 시각이 정확히 따라간다 (회귀 0)"
    - "currentMode 필드 + btnSelectAll/btnEdit BackColor + pictureBoxVideo.Cursor 가 단일 SetMode(DrawMode) 헬퍼를 통해 원자적으로 동기화된다"
  artifacts:
    - path: "Forms/MainForm.cs"
      provides: "private void SetMode(DrawMode mode) 헬퍼 + 4개 호출 지점 (btnSelectAll_Click, btnEdit_Click, LoadLabelingData reset, MainForm_Load)"
      contains: "private void SetMode(DrawMode mode)"
  key_links:
    - from: "btnSelectAll_Click / btnEdit_Click"
      to: "SetMode(DrawMode)"
      via: "method call"
      pattern: "SetMode\\(DrawMode\\.(Select|Draw)\\)"
    - from: "LoadLabelingData reset block (line ~675)"
      to: "SetMode(DrawMode.Select)"
      via: "method call"
      pattern: "SetMode\\(DrawMode\\.Select\\)"
    - from: "MainForm_Load (line ~173)"
      to: "SetMode(DrawMode.Select)"
      via: "초기 시각 동기화 1회 호출"
      pattern: "SetMode\\(DrawMode\\.Select\\)"
---

<objective>
FUNC-11 (KTC DF-2-05) 결함 수정: JSON 저장 자동 reload 사이클 후 좌측 모드 버튼의 시각 표시가 내부 currentMode 와 동기화되지 않아 '편집모드(0번)' 가 활성으로 보이는 문제를 해결한다.

`private void SetMode(DrawMode mode)` 헬퍼를 신규 도입하여 모드 변경 시 (1) `currentMode` 필드, (2) `btnSelectAll`/`btnEdit` 의 `BackColor`, (3) `pictureBoxVideo.Cursor` 를 한 곳에서 원자적으로 갱신한다. 기존 모드 변경 진입점 4개를 헬퍼 호출로 통일한다.

Purpose: KTC 2차 결함보고서 DF-2-05 의 "JSON 저장 시 좌측 모드 버튼 시각 불일치" 결함을 0건화. ISO/IEC 25023 기능적합성 + 사용성 (USAB) 충족.

Output: Forms/MainForm.cs 수정 — SetMode 헬퍼 추가 + 4개 호출 지점 일관화. 기능 추가 없음. JsonService.cs 수정 없음.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-json-저장-결함수정/07-CONTEXT.md
@CLAUDE.md
@Forms/MainForm.cs

<truths>
<!-- 모든 phase 7 plan 이 공유하는 제약 -->
- 새 기능 추가 금지 — 기존 결함 수정만 (PROJECT.md "기존 기능만 개선" 원칙)
- Services/JsonService.cs 수정 금지 (CONTEXT.md D-05) — 본 plan 은 UI 측 시각만 다룬다
- ISO/IEC 25023 8대 품질 특성 충족 / KISA 보안 가이드 준수 (CLAUDE.md)
- Critical/High 결함 0건 (CLAUDE.md)
- ARGB 활성 색상 = `Color.FromArgb(59, 130, 246)`, 비활성 = `Color.FromArgb(62, 62, 66)` (CONTEXT.md D-04)
- 한국어 주석/메시지 유지, PascalCase 메서드, ALL_CAPS 상수 (CONVENTIONS.md)
- DrawMode enum 은 `{ Select, Draw }` (Forms/MainForm.cs:20) — 추가하지 않음
</truths>

<interfaces>
<!-- Executor 가 그대로 사용해야 할 기존 시그니처 / 코드 모양 -->

From Forms/MainForm.cs:20 (namespace ASLTv1.Forms 외부):
```csharp
public enum DrawMode { Select, Draw }
```

From Forms/MainForm.cs:61 (현재 필드 기본값):
```csharp
private DrawMode currentMode = DrawMode.Select;
```

From Forms/MainForm.cs:1582-1596 (SetMode 추출 대상 — 현재 코드):
```csharp
private void btnSelectAll_Click(object sender, EventArgs e)
{
    currentMode = DrawMode.Select;
    btnSelectAll.BackColor = Color.FromArgb(59, 130, 246);
    btnEdit.BackColor = Color.FromArgb(62, 62, 66);
    pictureBoxVideo.Cursor = Cursors.Hand;
}

private void btnEdit_Click(object sender, EventArgs e)
{
    currentMode = DrawMode.Draw;
    btnEdit.BackColor = Color.FromArgb(59, 130, 246);
    btnSelectAll.BackColor = Color.FromArgb(62, 62, 66);
    pictureBoxVideo.Cursor = Cursors.Cross;
}
```

From Forms/MainForm.cs:670-680 (LoadLabelingData reset 블록 — `currentMode = DrawMode.Select;` 한 줄을 SetMode 로 교체):
```csharp
// FUNC-09: 이전 작업 상태 완전 초기화
undoStack.Clear();
redoStack.Clear();
entryFrameIndex = null;
exitFrameIndex = null;
currentMode = DrawMode.Select;   // ← 이 줄을 SetMode(DrawMode.Select); 로 교체
currentAssignedId = 1;
```

From Forms/MainForm.cs:173 (MainForm_Load — 끝에 SetMode 1회 호출 추가):
```csharp
private void MainForm_Load(object sender, EventArgs e)
{
    this.WindowState = FormWindowState.Maximized;
    DarkTheme.Apply(this);
    btnSelectFolder.Text = "파일 선택";
    // ... (기존 코드)
}
```

From Forms/MainForm.cs:2913-2914 (단축키 D1/D2 — **수정하지 않음**, btnSelectAll_Click / btnEdit_Click 호출이 SetMode 적용 후 자동 동기화됨):
```csharp
else if (e.KeyCode == Keys.D1 && !e.Control && !e.Alt) { btnSelectAll_Click(sender, e); e.Handled = true; }
else if (e.KeyCode == Keys.D2 && !e.Control && !e.Alt) { btnEdit_Click(sender, e); e.Handled = true; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SetMode 헬퍼 도입 + 4개 호출 지점 통일</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/07-json-저장-결함수정/07-CONTEXT.md (전체 — 특히 D-01..D-04)
    - Forms/MainForm.cs:55-75 (UI State region 필드 — currentMode 기본값 위치)
    - Forms/MainForm.cs:170-210 (MainForm_Load — 초기 SetMode 호출 삽입 지점)
    - Forms/MainForm.cs:660-700 (LoadLabelingData reset 블록 — line 675)
    - Forms/MainForm.cs:1578-1600 (Drawing/Painting region 시작부 + btnSelectAll_Click / btnEdit_Click)
    - Forms/MainForm.cs:2905-2920 (D1/D2 단축키 — 수정 안 함, 동작 확인용)
  </read_first>
  <behavior>
    - SetMode(DrawMode.Select) 호출 후: currentMode == DrawMode.Select; btnSelectAll.BackColor == Color.FromArgb(59, 130, 246); btnEdit.BackColor == Color.FromArgb(62, 62, 66); pictureBoxVideo.Cursor == Cursors.Hand
    - SetMode(DrawMode.Draw) 호출 후: currentMode == DrawMode.Draw; btnEdit.BackColor == Color.FromArgb(59, 130, 246); btnSelectAll.BackColor == Color.FromArgb(62, 62, 66); pictureBoxVideo.Cursor == Cursors.Cross
    - btnSelectAll_Click / btnEdit_Click 호출 시 위 3가지 상태가 일관되게 적용됨
    - LoadLabelingData reset 경로에서 `currentMode = DrawMode.Select` 만 갱신되던 것이 시각까지 동시 갱신됨
    - MainForm_Load 직후 시각이 currentMode 기본값(Select)과 일치 (초기 잔존 표시 없음)
  </behavior>
  <action>
1. **SetMode 헬퍼 추가** — Drawing/Painting region 내, btnSelectAll_Click 직전 위치 (line ~1582 직전) 에 다음 헬퍼를 추가한다. ARGB 상수는 헬퍼 내부 `private static readonly` 로 묶어 inline 중복을 제거한다:

```csharp
// FUNC-11 (DF-2-05): 모드 전환 시 currentMode + 좌측 버튼 BackColor + Cursor 를
// 한 곳에서 원자적으로 갱신하여 시각 동기화 결함 방지.
// (ARGB: 활성 = (59, 130, 246) — 파란색, 비활성 = (62, 62, 66) — 다크 톤)
private static readonly Color ModeButtonActiveColor   = Color.FromArgb(59, 130, 246);
private static readonly Color ModeButtonInactiveColor = Color.FromArgb(62, 62, 66);

private void SetMode(DrawMode mode)
{
    currentMode = mode;

    if (mode == DrawMode.Select)
    {
        btnSelectAll.BackColor = ModeButtonActiveColor;
        btnEdit.BackColor      = ModeButtonInactiveColor;
        pictureBoxVideo.Cursor = Cursors.Hand;
    }
    else // DrawMode.Draw
    {
        btnEdit.BackColor      = ModeButtonActiveColor;
        btnSelectAll.BackColor = ModeButtonInactiveColor;
        pictureBoxVideo.Cursor = Cursors.Cross;
    }
}
```

2. **btnSelectAll_Click 단순화** (line 1582-1588) — 본문 4줄을 `SetMode(DrawMode.Select);` 한 줄로 교체:

```csharp
private void btnSelectAll_Click(object sender, EventArgs e)
{
    SetMode(DrawMode.Select);
}
```

3. **btnEdit_Click 단순화** (line 1590-1596) — 본문 4줄을 `SetMode(DrawMode.Draw);` 한 줄로 교체:

```csharp
private void btnEdit_Click(object sender, EventArgs e)
{
    SetMode(DrawMode.Draw);
}
```

4. **LoadLabelingData reset 블록 갱신** (line 675) — `currentMode = DrawMode.Select;` 한 줄을 `SetMode(DrawMode.Select);` 로 교체. 이 한 줄 변경이 JSON 저장 → 자동 reload 사이클에서 좌측 버튼 시각이 정상 동기화되도록 만드는 핵심 fix 다 (DF-2-05 의 직접 원인).

5. **MainForm_Load 초기 시각 동기화** — `MainForm_Load(object sender, EventArgs e)` (line 173) 메서드 본문에서 `DarkTheme.Apply(this);` (line 176) 호출 **직후** 한 줄 `SetMode(DrawMode.Select);` 추가. 폼 최초 표시 시점부터 currentMode 기본값과 시각이 일치하도록 보장. (이 호출은 currentMode 기본값과 동일하므로 상태 변화는 없으나, 시각 측면에서 누락되어 있던 초기화를 보강한다.)

6. **필드 기본값** (line 61 `private DrawMode currentMode = DrawMode.Select;`) — **수정하지 않음**. CONTEXT.md D-02 가 그대로 유지하라고 명시.

7. **단축키 D1/D2** (line 2913-2914) — **수정하지 않음**. 기존 핸들러를 호출하므로 SetMode 효과가 자동 전파됨 (D-03).

8. 모든 한국어 주석은 기존 톤 유지 (CONVENTIONS.md).
  </action>
  <verify>
    <automated>
      # 1. 빌드 성공
      dotnet build C:/Users/ANNA/AOLTv1.0/ASLTv1.0.csproj -c Debug -v minimal
      # 2. SetMode 헬퍼 정확히 1개 정의됨
      grep -c "private void SetMode(DrawMode mode)" Forms/MainForm.cs   # == 1
      # 3. SetMode 호출 지점 4개 (Click 핸들러 2 + LoadLabelingData reset 1 + MainForm_Load 1)
      grep -c "SetMode(DrawMode\." Forms/MainForm.cs                     # == 4
      # 4. inline ARGB(59, 130, 246) BackColor 직접 할당 검증 — baseline-and-delta:
      #    현재(fix 전) 베이스라인: 정확히 2개 등장 (btnSelectAll_Click + btnEdit_Click 의 BackColor = ... line)
      #    fix 후 기대: 0개 (둘 다 SetMode 호출로 교체되고, 헬퍼 내부는 ModeButtonActiveColor 상수를 통해 갱신)
      grep -v '^[[:space:]]*//' Forms/MainForm.cs | grep -c "BackColor = Color.FromArgb(59, 130, 246)"   # == 0 (delta -2 from baseline)
      # 4b. ARGB 활성 색상 상수 정의 정확히 1번 (헬퍼 도입 검증)
      grep -c "Color.FromArgb(59, 130, 246)" Forms/MainForm.cs   # == 1 (ModeButtonActiveColor 정의 1개)
      # 5. ModeButton 상수 도입됨
      grep -c "ModeButtonActiveColor\|ModeButtonInactiveColor" Forms/MainForm.cs   # >= 4 (정의 2 + 사용 2 이상)
      # 6. LoadLabelingData reset 경로가 SetMode 호출하도록 변경됨 — line 675 인근
      grep -n "SetMode(DrawMode.Select)" Forms/MainForm.cs   # 최소 2 매치 (reset + MainForm_Load)
      # 7. JsonService.cs 미수정 (D-05 가드)
      git diff --name-only | grep -v "^Services/JsonService.cs$" >/dev/null && echo "OK: JsonService.cs untouched"
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] `dotnet build` 성공 (warning 0 증가, error 0)
    - [ ] `grep -c "private void SetMode(DrawMode mode)" Forms/MainForm.cs` == 1
    - [ ] `grep -c "SetMode(DrawMode\." Forms/MainForm.cs` == 4
    - [ ] btnSelectAll_Click 본문이 `SetMode(DrawMode.Select);` 단일 statement (3줄 inline 제거됨)
    - [ ] btnEdit_Click 본문이 `SetMode(DrawMode.Draw);` 단일 statement
    - [ ] line 675 reset 블록의 `currentMode = DrawMode.Select;` 가 `SetMode(DrawMode.Select);` 로 교체됨
    - [ ] MainForm_Load 본문에 `SetMode(DrawMode.Select);` 호출 1개 추가됨 (DarkTheme.Apply 직후)
    - [ ] `ModeButtonActiveColor` / `ModeButtonInactiveColor` 상수 정의됨, 헬퍼 내부에서 사용
    - [ ] currentMode 필드 기본값(line 61) 변경되지 않음 (`DrawMode.Select` 그대로)
    - [ ] D1/D2 단축키 코드(line 2913-2914) 변경되지 않음
    - [ ] Services/JsonService.cs 변경되지 않음 (`git diff --stat Services/JsonService.cs` 비어있음)
    - [ ] 헬퍼 추가 위치는 Drawing/Painting region 내, btnSelectAll_Click 직전
    - [ ] 한국어 주석 톤이 기존 코드와 일치 (// FUNC-11 (DF-2-05) prefix 사용)
  </acceptance_criteria>
  <done>
    빌드 성공 + 모든 acceptance criteria 통과. SetMode 헬퍼가 단일 진실의 원천이 되어 4개 호출 지점이 모두 동일한 동작을 수행. 회귀 테스트 (수동) 는 07-03 plan 에서 실행.
  </done>
</task>

</tasks>

<verification>
**자동 검증** (Task 1 verify 블록 참조)

**수동 회귀 검증** (이 plan 종료 시점에는 빌드 성공만으로 충분 — 통합 회귀는 07-03 plan):
1. 디버그 빌드 후 ASLTv1.exe 실행 → 좌측 패널 확인: btnSelectAll 이 파란색(활성), btnEdit 가 다크 톤(비활성)
2. btnEdit 클릭 → btnEdit 파란색, btnSelectAll 다크 톤. 커서가 Cross 로 변경
3. btnSelectAll 클릭 → btnSelectAll 파란색, btnEdit 다크 톤. 커서가 Hand 로 변경
4. D1/D2 단축키 동일 동작
5. JSON 로드 → reset 사이클 통과 후 좌측 시각이 '전체선택' 활성 표시 (편집모드 잔존 표시 0건)

**Out of scope:** JSON 저장 1-BBOX 가드 (FUNC-12 → 07-02 plan), 인스톨러 빌드 (07-03 plan)
</verification>

<success_criteria>
- [ ] FUNC-11 acceptance criteria 4개 (REQUIREMENTS.md) 모두 충족
- [ ] Forms/MainForm.cs 외 파일 변경 0
- [ ] 빌드 warning 0 증가
- [ ] CONTEXT.md D-01..D-04 모두 반영 (helper 도입 / 4개 호출지점 통일 / 단축키 비수정 / ARGB 헬퍼 내부 캡슐화)
</success_criteria>

<output>
완료 후 `.planning/phases/07-json-저장-결함수정/07-01-SUMMARY.md` 작성:
- 변경 파일/라인 요약
- SetMode 헬퍼 시그니처
- 4개 호출 지점 인용
- 빌드 로그 요약
- FUNC-11 acceptance criteria 체크리스트 결과
</output>
