---
phase: 07-json-저장-결함수정
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - installer/Output/ASLT-Setup-v1.0.3.exe
autonomous: false
requirements:
  - FUNC-11
  - FUNC-12
tags:
  - regression
  - installer
  - upgrade-verify
  - audit-log
must_haves:
  truths:
    - "FUNC-11 결함 (DF-2-05) 이 빌드된 1.0.3 바이너리에서 재현되지 않는다 — JSON 저장 후 좌측 모드 버튼이 '전체선택' 활성 시각으로 정확히 표시"
    - "FUNC-12 결함 (DF-2-06) 이 빌드된 1.0.3 바이너리에서 재현되지 않는다 — BBOX 1개 + Waypoint 없음 케이스에서 저장 차단 + 안내 메시지"
    - "v1.0.2 에서 통과한 JSON 저장 시나리오 (타임스탬프, 카테고리 매핑, BBOX ≥ 2개 정상 케이스) 가 회귀 없이 동작"
    - "ASLT-Setup-v1.0.3.exe 가 installer/Output/ 에 생성되며 실행 시 1.0.2 에서 1.0.3 으로 in-place 업그레이드가 성공"
    - "JSON 저장 시 [AUDIT] 로그 엔트리가 정상 기록되고 HMAC 체인 무결성이 유지된다"
  artifacts:
    - path: "installer/Output/ASLT-Setup-v1.0.3.exe"
      provides: "v1.0.3 인스톨러 (Inno Setup 6 self-contained)"
      contains: "ASLT-Setup-v1.0.3"
    - path: ".planning/phases/07-json-저장-결함수정/07-03-SUMMARY.md"
      provides: "회귀 테스트 결과 + 인스톨러 빌드 로그 + in-place 업그레이드 검증 결과"
      contains: "회귀 검증"
  key_links:
    - from: "installer/build-installer.ps1"
      to: "installer/Output/ASLT-Setup-v1.0.3.exe"
      via: "Inno Setup ISCC.exe 컴파일"
      pattern: "ASLT-Setup-v1.0.3.exe"
    - from: "1.0.2 설치 상태"
      to: "1.0.3 in-place 업그레이드"
      via: "AppId pinned (CONTEXT.md D-12)"
      pattern: "in-place"
    - from: "JSON 저장 동작"
      to: "%LOCALAPPDATA%\\ANNA\\ASLT\\logs\\ASLT-yyyy-MM-dd.log"
      via: "[AUDIT] entry + HmacChainSink"
      pattern: "\\[AUDIT\\].*JSON"
---

<objective>
Phase 7 종료 verification — 07-01 (FUNC-11) + 07-02 (FUNC-12) fix 가 적용된 코드를 실제 1.0.3 인스톨러로 빌드하고, 회귀 시나리오 + in-place 업그레이드 + 감사 로그 무결성을 모두 검증한다.

이 plan 은 코드 변경 없음 — 빌드 + 실행 + 검증만 수행. 결과는 07-03-SUMMARY.md 에 정리하여 retrospective + 인증 감사 트레이스에 사용.

Purpose: KTC 2차 결함 2건이 1.0.3 바이너리에서 0건임을 입증 + 1.0.0 → 1.0.1 → 1.0.2 → 1.0.3 업그레이드 체인 보장 + ISO/IEC 25023 신뢰성/이식성 항목 회귀 0 확인.

Output: ASLT-Setup-v1.0.3.exe (installer/Output/) + 07-03-SUMMARY.md (검증 결과 보고서).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-json-저장-결함수정/07-CONTEXT.md
@.planning/phases/07-json-저장-결함수정/07-01-PLAN.md
@.planning/phases/07-json-저장-결함수정/07-02-PLAN.md
@CLAUDE.md
@installer/build-installer.ps1
@ASLTv1.0.csproj

<truths>
- 코드 변경 없음 — 본 plan 은 build/test/verify 만 수행
- 인스톨러 build script 는 기존 `installer/build-installer.ps1` 그대로 사용 (CONTEXT.md D-12)
- AppId pinned 유지 — 1.0.2 → 1.0.3 in-place 업그레이드 보장
- 버전은 이미 csproj 에서 1.0.3 으로 bump 되어 있음 (commit c5d8102)
- 인증 감사 트레이스: 회귀 결과 + 빌드 로그를 SUMMARY 에 보존 (KTC 1차/2차 결함수정 사이클 동일 형식)
</truths>

<interfaces>
From installer/build-installer.ps1 (7단계 자동화):
1. Stop-Process ASLTv1
2. csproj Version 추출 (검증용)
3. clean — bin/Release + installer/Output/*.exe (ffmpeg/ 미손상)
4. dotnet publish -c Release -r win-x64 --self-contained true
5. publish 산출물 검증 (ASLTv1.exe + OpenCvSharpExtern.dll)
6. ISCC.exe 컴파일 (Inno Setup 6)
7. 인스톨러 검증 — `installer/Output/ASLT-Setup-v$Version.exe` 존재 확인

Expected output filename: `installer/Output/ASLT-Setup-v1.0.3.exe`

From ASLTv1.0.csproj (현재 상태 — 이미 1.0.3 bump 되어 있어야 함):
- `<Version>1.0.3</Version>` (또는 `<AssemblyVersion>` / `<FileVersion>`)

Audit log location:
- `%LOCALAPPDATA%\ANNA\ASLT\logs\ASLT-yyyy-MM-dd.log`
- HmacChainSink 가 [AUDIT] prefix 엔트리를 기록 — JSON 저장 이벤트 포함
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: 1.0.2 인스톨러 백업 (Task 3 in-place 업그레이드 검증용 사전조건)</name>
  <what-built>
    아직 빌드 전. 본 task 는 Task 3 (1.0.2 → 1.0.3 in-place 업그레이드 검증) 의 사전조건 — 빌드 스크립트가 installer/Output/ 의 기존 .exe 를 삭제(clean step 3) 하므로, 1.0.2 인스톨러를 사용 가능한 위치에 별도 보관해야 한다.
  </what-built>
  <how-to-verify>
**현재 상태 확인:**
- `installer/Output/` 내용을 ls 로 확인했을 때 `ASLT-Setup-v1.0.2.exe` 가 있는가?
  - 없으면 (확인된 사실 — 현재 dir 에는 stale `ASLT-Setup-v1.0.3.exe` 만 있음) → 사용자가 외부 백업/배포 위치에서 1.0.2 인스톨러를 복원해야 한다.

**사용자가 수행할 사전 조치 — 다음 중 하나:**

옵션 A — 사용자가 1.0.2 인스톨러 사본을 가지고 있는 경우:
  1. ASLT-Setup-v1.0.2.exe 를 다음 백업 경로에 복사:
     `C:\Users\ANNA\AOLTv1.0\installer\Output\_backup\ASLT-Setup-v1.0.2.exe`
  2. Task 3 의 U-1 단계에서 이 백업 파일을 사용하여 clean install
  3. 본 task 의 resume-signal 에 "approved — 백업 경로: <full path>" 입력

옵션 B — 1.0.2 인스톨러를 더 이상 가지고 있지 않은 경우:
  1. v1.0.2 태그 또는 commit 0ff6199 에서 코드를 체크아웃하고 `installer/build-installer.ps1` 실행하여 1.0.2 재빌드
  2. 결과물을 `C:\Users\ANNA\AOLTv1.0\installer\Output\_backup\ASLT-Setup-v1.0.2.exe` 로 보관
  3. 메인 브랜치로 복귀 (`git checkout main`)
  4. 본 task 의 resume-signal 에 "approved — 재빌드 경로: <full path>" 입력

옵션 C — 1.0.2 가 환경에 이미 설치되어 있고 인스톨러 파일은 없는 경우:
  1. Task 3 의 U-1 (clean install) 단계는 생략, U-2 (현재 상태가 1.0.2 임을 확인) 부터 시작
  2. SUMMARY 에 "clean-install 검증 생략 — 기존 1.0.2 설치본 재사용" 명시
  3. 본 task 의 resume-signal 에 "approved — 옵션 C" 입력

**검증 토큰:**
- 옵션 A/B 선택 시: `Test-Path C:\Users\ANNA\AOLTv1.0\installer\Output\_backup\ASLT-Setup-v1.0.2.exe` == True
- 옵션 C 선택 시: 사용자 승인만 필요 (파일 검증 없음)
  </how-to-verify>
  <resume-signal>
    Type "approved — 옵션 A" / "approved — 옵션 B" / "approved — 옵션 C" 중 하나.
    옵션 A/B 선택 시 백업 파일 절대 경로 함께 보고.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1: csproj 버전 확인 + 인스톨러 빌드</name>
  <files>installer/Output/ASLT-Setup-v1.0.3.exe</files>
  <read_first>
    - .planning/phases/07-json-저장-결함수정/07-CONTEXT.md (D-12 — 인스톨러 빌드 가이드)
    - installer/build-installer.ps1 (전체 — 7단계 자동화)
    - ASLTv1.0.csproj (Version 필드 확인)
    - .planning/STATE.md (현재 milestone 상태)
  </read_first>
  <behavior>
    - csproj Version == "1.0.3" 확인 (이미 commit c5d8102 에서 bump 완료)
    - 만약 Version 이 1.0.3 이 아니면 즉시 중단하고 사용자에게 보고 (자동 bump 금지 — 의사결정 영역)
    - build-installer.ps1 7단계 모두 통과
    - installer/Output/ASLT-Setup-v1.0.3.exe 생성 + 파일 크기 50MB 이상
  </behavior>
  <action>
1. **csproj 버전 사전 확인** — 다음 명령으로 현재 Version 읽기:
   ```powershell
   powershell -Command "([xml](Get-Content C:/Users/ANNA/AOLTv1.0/ASLTv1.0.csproj)).Project.PropertyGroup.Version"
   ```
   기대값: `1.0.3`. 다른 값이면 즉시 중단하고 사용자 확인.

2. **빌드 스크립트 실행** — 작업 디렉토리를 프로젝트 루트로 두고:
   ```powershell
   powershell -ExecutionPolicy Bypass -File C:/Users/ANNA/AOLTv1.0/installer/build-installer.ps1
   ```
   7단계 출력을 캡처하여 SUMMARY 에 인용 가능하게 보존.

3. **빌드 결과 검증**:
   - `installer/Output/ASLT-Setup-v1.0.3.exe` 존재 확인
   - 파일 크기 ≥ 50MB (1.0.2 가 98.19MB 였으므로 비슷한 크기 예상)
   - 빌드 로그에 "Build Successful" 출력 확인

4. **빌드 실패 처리**:
   - dotnet publish 실패 (LASTEXITCODE != 0) → 빌드 로그 첨부 + 중단
   - ISCC 누락 (`C:\Program Files (x86)\Inno Setup 6\ISCC.exe` 없음) → 사용자에게 환경 설정 요청
   - 그 외 throw → 스크립트 출력 그대로 SUMMARY 에 기록
  </action>
  <verify>
    <automated>
      # 1. csproj 버전이 1.0.3 인지
      powershell -Command "([xml](Get-Content C:/Users/ANNA/AOLTv1.0/ASLTv1.0.csproj)).Project.PropertyGroup.Version" | grep -c "^1.0.3$"   # == 1
      # 2. 인스톨러 산출물 존재
      ls C:/Users/ANNA/AOLTv1.0/installer/Output/ASLT-Setup-v1.0.3.exe   # 파일 발견
      # 3. 인스톨러 크기 확인 (>= 50MB)
      powershell -Command "(Get-Item C:/Users/ANNA/AOLTv1.0/installer/Output/ASLT-Setup-v1.0.3.exe).Length / 1MB" | awk '{ if ($1 < 50) exit 1 }'
      # 4. 1.0.2 인스톨러도 별도 보관 (in-place 업그레이드 테스트용)
      ls C:/Users/ANNA/AOLTv1.0/installer/Output/   # 출력 확인 — v1.0.3 이 production
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] csproj Version == "1.0.3" 확인 (변경 없음)
    - [ ] `installer/build-installer.ps1` 7단계 모두 통과 — exit code 0
    - [ ] `installer/Output/ASLT-Setup-v1.0.3.exe` 생성됨
    - [ ] 인스톨러 크기 ≥ 50MB (sanity — self-contained 빌드 누락 검출)
    - [ ] 빌드 로그에 "Build Successful" 표시
    - [ ] dotnet publish + ISCC 컴파일 모두 성공 (exit code 0)
    - [ ] 빌드 로그가 SUMMARY 에 인용 가능한 형태로 보존됨
  </acceptance_criteria>
  <done>
    ASLT-Setup-v1.0.3.exe 가 installer/Output/ 에 존재하고, 빌드 스크립트 7단계 모두 성공. 빌드 로그가 SUMMARY 작성용으로 보존됨.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: FUNC-11 / FUNC-12 결함 재현 + 회귀 시나리오 (수동 UAT)</name>
  <what-built>
    - SetMode(DrawMode) 헬퍼 도입 + 4개 호출 지점 통일 (07-01)
    - btnExportJson_Click 1-BBOX 가드 추가 (07-02)
    - ASLT-Setup-v1.0.3.exe 빌드 (Task 1)
  </what-built>
  <how-to-verify>
**준비:** 깨끗한 Windows 환경에서 ASLT-Setup-v1.0.3.exe 를 실행하여 새로 설치 (또는 기존 1.0.2 위에 in-place 업그레이드 — Task 3 에서 별도 검증). 임의의 영상 파일 1개 (`*.mp4` 또는 `*.avi`) 준비.

**Scenario A — FUNC-11 (DF-2-05) 결함 비재현 검증**

A-1. ASLTv1.exe 실행 → 좌측 패널 확인
   - 기대: btnSelectAll(1번 전체선택) 파란색(활성), btnEdit(0번 편집) 다크 톤(비활성)
   - 결과: ☐ Pass / ☐ Fail (실제 시각: ____)

A-2. 영상 로드 → 임의 프레임에서 person BBOX 2개 그림 (서로 다른 프레임)
   - 모드 전환 동작 확인: btnEdit 클릭 → btnEdit 파란색 / btnSelectAll 다크 톤
   - 결과: ☐ Pass / ☐ Fail

A-3. JSON 저장 (메뉴 또는 헤더 버튼 클릭) → 저장 완료 후 자동 reload
   - **핵심 확인**: 저장 후 좌측 모드 버튼이 btnSelectAll(1번 전체선택) **파란색** 활성 시각으로 표시되는가? (편집모드 잔존 표시 0건)
   - 결과: ☐ Pass / ☐ Fail (실제: ____)

A-4. 단축키 D1/D2 회귀
   - D2 누름 → btnEdit 파란색 / 커서 Cross
   - D1 누름 → btnSelectAll 파란색 / 커서 Hand
   - 결과: ☐ Pass / ☐ Fail

**Scenario B — FUNC-12 (DF-2-06) 결함 비재현 검증**

B-1. 영상 로드 → 임의 프레임에서 person BBOX **1개만** 그림 (다른 프레임에 추가 BBOX 없음)
   - JSON 저장 시도 (메뉴 호출)
   - **핵심 확인**: 저장 다이얼로그가 표시되지 않고, 다음 메시지가 표시되는가?
     - "Exit 는 Entry 이후 프레임에서 설정해주세요. 하나의 BBOX 만 존재하는 객체가 있어 Waypoint 를 생성할 수 없습니다."
     - "해당 객체: [person] [01]" (또는 실제 ID)
     - "해결 방법: 해당 객체의 다른 프레임에서 BBOX 를 추가하거나, Entry/Exit 를 명시적으로 설정한 후 다시 저장해주세요."
   - 결과: ☐ Pass / ☐ Fail

B-2. (회귀 1 — 정상 케이스) 같은 객체에 다른 프레임에서 BBOX 1개 추가 (총 2개 — Entry/Exit 가능)
   - JSON 저장 → 저장 다이얼로그 표시 + 정상 저장
   - 저장된 JSON 파일 내 Waypoint 의 Entry != Exit 확인 (텍스트 에디터로 열어 `entry.frame` 와 `exit.frame` 다름 확인)
   - 결과: ☐ Pass / ☐ Fail

B-3. (회귀 2 — 명시적 Waypoint) 새 영상 로드 → person BBOX 1개 그림 → E 키 (Entry 설정) → 다른 프레임으로 이동 → X 키 (Exit + Waypoint 생성)
   - 이 상태에서 JSON 저장 → 정상 저장 (가드 통과 — 명시적 Waypoint 가 BBOX 1개를 커버)
   - 결과: ☐ Pass / ☐ Fail

B-4. (회귀 3 — 0-BBOX 가드 미회귀) 영상만 로드 + BBOX/Waypoint 0개 + 기존 JSON 없음
   - JSON 저장 → "저장할 라벨링 데이터가 없어 JSON 파일은 생성되지 않습니다." 메시지 (기존 0-BBOX 가드 동작 유지)
   - 결과: ☐ Pass / ☐ Fail

**Scenario C — JSON 저장 일반 회귀**

C-1. 영상 로드 → person 2개, vehicle 1개, event 1개 (각각 BBOX ≥ 2개 + Waypoint) 라벨링
   - JSON 저장 → 정상 저장 → JSON 파일 열어 다음 확인:
     - `images` 배열에 frame_id + timestamp 정상
     - `categories` 매핑 정상 (person/vehicle/event 카테고리 ID)
     - `annotations[].track_info.entry.frame` < `annotations[].track_info.exit.frame` 모든 Waypoint
   - 결과: ☐ Pass / ☐ Fail

C-2. 동일 JSON 재로드 → 좌측 시각이 '전체선택' 활성 (FUNC-11 자동 reload 사이클 회귀 0)
   - 결과: ☐ Pass / ☐ Fail

  </how-to-verify>
  <resume-signal>
    Type "approved" if all scenarios pass.
    Type a description if any scenario fails (will trigger gap-closure or revision).
    Required answers in SUMMARY: A-1..A-4, B-1..B-4, C-1, C-2 결과 표기.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 1.0.2 → 1.0.3 in-place 업그레이드 검증 (수동)</name>
  <what-built>
    - ASLT-Setup-v1.0.3.exe (Task 1)
    - AppId pinned 유지 — Inno Setup 6 in-place 업그레이드 모드 (CONTEXT.md D-12)
  </what-built>
  <how-to-verify>
**준비 (Task 0 에서 확정된 옵션 사용):**
- 옵션 A/B: `C:\Users\ANNA\AOLTv1.0\installer\Output\_backup\ASLT-Setup-v1.0.2.exe` 사용 (Task 0 에서 사용자가 백업)
- 옵션 C: 환경에 이미 설치된 1.0.2 사용 → U-1 생략, U-2 부터 시작
- 깨끗한 Windows 환경 (또는 1.0.x 가 미설치된 환경) — 옵션 A/B 만 해당

**검증 단계:**

U-1. 1.0.2 설치 (clean install)
   - ASLT-Setup-v1.0.2.exe 실행 → 정상 설치
   - ASLTv1.exe 실행 → 영상 로드 → 라벨링 1건 + JSON 저장 (의도적으로 사용자 흔적 남기기)
   - 온보딩 완료 (시작 가이드 닫기) → SettingsService 가 `%LOCALAPPDATA%\ANNA\ASLT\settings.json` 에 영속화
   - 종료
   - 결과: ☐ Pass / ☐ Fail

U-2. 설치 폴더 + 사용자 설정 확인
   - `C:\Program Files\ANNA\ASLT\ASLTv1.exe` 버전 메타데이터 = 1.0.2
   - `%LOCALAPPDATA%\ANNA\ASLT\settings.json` 존재 + 온보딩 완료 상태
   - `%LOCALAPPDATA%\ANNA\ASLT\logs\ASLT-yyyy-MM-dd.log` 존재 (HMAC chain 활성)
   - 결과: ☐ Pass / ☐ Fail

U-3. 1.0.3 in-place 업그레이드
   - **언인스톨하지 않고** ASLT-Setup-v1.0.3.exe 실행
   - 인스톨러가 "이미 설치됨 — 업그레이드" 모드로 진행 (AppId 일치)
   - 설치 완료
   - 결과: ☐ Pass / ☐ Fail

U-4. 업그레이드 후 상태 검증
   - `C:\Program Files\ANNA\ASLT\ASLTv1.exe` 버전 메타데이터 = **1.0.3** (실행파일 교체됨)
   - `%LOCALAPPDATA%\ANNA\ASLT\settings.json` **변경되지 않음** (사용자 설정 보존)
   - 온보딩 상태 보존 — ASLTv1.exe 실행 시 시작 가이드 미표시
   - 기존 `logs/` 디렉토리 보존 (HMAC chain 연속성)
   - 결과: ☐ Pass / ☐ Fail

U-5. 업그레이드 후 동작 검증
   - ASLTv1.exe 실행 → U-1 에서 라벨링한 영상 다시 열어 JSON 로드 → 라벨링 데이터 정상 표시
   - About 창 (또는 헤더) 에서 버전 = 1.0.3 표시
   - JSON 저장 (FUNC-11 + FUNC-12 fix 적용된 빌드) → 정상 동작
   - 결과: ☐ Pass / ☐ Fail
  </how-to-verify>
  <resume-signal>
    Type "approved" if U-1..U-5 모두 pass.
    Failure 시 specific step + observed behavior 보고 (롤백 필요 여부 결정).
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: 감사 로그 무결성 확인</name>
  <files>(read-only — %LOCALAPPDATA%/ANNA/ASLT/logs/ 검증)</files>
  <read_first>
    - .planning/PROJECT.md (HMAC chain 정책)
    - Services/HmacChainSink.cs (참고 — 변경 안 함)
    - Services/LogIntegrityVerifier.cs (참고 — 변경 안 함)
  </read_first>
  <behavior>
    - Task 2/3 의 JSON 저장 행위가 [AUDIT] prefix 로 로그에 기록됨
    - HMAC chain 이 일중 + 일간 연속성을 유지함 (LogIntegrityVerifier 가 검증)
    - 1-BBOX 가드 발동(Task 2 B-1) 의 경우에는 저장 자체가 발생하지 않으므로 [AUDIT] JSON 저장 엔트리가 기록되지 않음 (정상)
  </behavior>
  <action>
1. 로그 디렉토리 확인:
   ```powershell
   ls $env:LOCALAPPDATA\ANNA\ASLT\logs\
   ```

2. 오늘 자 로그 파일에서 [AUDIT] JSON 관련 엔트리 추출:
   ```powershell
   $today = Get-Date -Format "yyyy-MM-dd"
   Select-String -Path "$env:LOCALAPPDATA\ANNA\ASLT\logs\ASLT-$today.log" -Pattern "\[AUDIT\].*JSON"
   ```

3. **확인 항목** (모두 SUMMARY 에 인용):
   - Task 2 의 B-2 (정상 저장) 시점에 [AUDIT] JSON 저장 엔트리 1건 이상 존재
   - Task 2 의 B-3 (명시적 Waypoint 정상 저장) 시점에 [AUDIT] JSON 저장 엔트리 1건 이상 존재
   - Task 2 의 C-1 (다중 객체 정상 저장) 시점에 [AUDIT] JSON 저장 엔트리 1건 이상 존재
   - Task 2 의 B-1 (1-BBOX 가드 발동) 시점에는 저장 [AUDIT] 엔트리가 **없음** (가드 발동 시 저장이 차단되므로 정상)
   - Task 3 의 U-5 (업그레이드 후 저장) 시점 [AUDIT] 엔트리 존재
   - 로그 파일이 비어있지 않고, HMAC chain prefix/suffix 토큰이 각 라인에 존재 (HmacChainSink 형식)

4. **HMAC chain 무결성 — 비공식 sanity check**:
   - 로그 파일이 정상적으로 파싱 가능 (Serilog 형식)
   - 라인 끝의 HMAC 토큰이 비어있지 않음
   - 정밀 검증은 LogIntegrityVerifier 별도 도구로 가능 (인증 감사 시 사용 — 본 plan scope 밖)
  </action>
  <verify>
    <automated>
      # 1. 로그 디렉토리 존재
      powershell -Command "Test-Path $env:LOCALAPPDATA\ANNA\ASLT\logs\"
      # 2. 오늘자 로그 파일 존재 + 비어있지 않음
      powershell -Command "$today = Get-Date -Format 'yyyy-MM-dd'; (Get-Item $env:LOCALAPPDATA\ANNA\ASLT\logs\ASLT-$today.log).Length -gt 0"
      # 3. [AUDIT] JSON 관련 엔트리 존재 (Task 2/3 에서 정상 저장한 결과)
      powershell -Command "$today = Get-Date -Format 'yyyy-MM-dd'; (Select-String -Path $env:LOCALAPPDATA\ANNA\ASLT\logs\ASLT-$today.log -Pattern '\[AUDIT\].*JSON' -SimpleMatch:$false).Count -ge 1"
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] 오늘자 로그 파일 존재 + 비어있지 않음
    - [ ] Task 2/3 의 정상 저장 행위가 [AUDIT] prefix 로 기록됨 (최소 3건 이상)
    - [ ] Task 2 B-1 (가드 발동) 시점에 저장 [AUDIT] 엔트리 없음 (저장 차단 정상)
    - [ ] HMAC chain 토큰이 모든 로그 라인에 정상 부착됨 (sanity)
    - [ ] in-place 업그레이드 후 (1.0.2 → 1.0.3) 로그 파일 손상 없음 — 동일 파일에 1.0.3 이 append 가능
  </acceptance_criteria>
  <done>
    감사 로그 정상 기록 + HMAC chain 무결성 sanity 통과. 인용 가능한 [AUDIT] 엔트리 샘플이 SUMMARY 에 보존.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: 07-03-SUMMARY.md 작성 + Phase 7 클로즈아웃</name>
  <files>.planning/phases/07-json-저장-결함수정/07-03-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY 템플릿)
    - .planning/phases/07-json-저장-결함수정/07-01-SUMMARY.md (있으면)
    - .planning/phases/07-json-저장-결함수정/07-02-SUMMARY.md (있으면)
    - .planning/REQUIREMENTS.md (FUNC-11/12 acceptance criteria — 매핑용)
    - .planning/ROADMAP.md (Phase 7 success criteria 4건 — 매핑용)
    - Task 1-4 의 출력 (빌드 로그, 회귀 결과, 업그레이드 결과, 감사 로그 샘플)
  </read_first>
  <behavior>
    - SUMMARY 가 표준 템플릿 따름 (objective / what-changed / verification / next-steps)
    - Task 2/3 의 모든 시나리오 결과가 명시적으로 인용됨 (A-1..A-4, B-1..B-4, C-1, C-2, U-1..U-5)
    - Phase 7 ROADMAP success criteria 4건 매핑 (FUNC-11 / FUNC-12 / 회귀 / 인스톨러 + 업그레이드)
    - 빌드 로그 핵심 발췌 (publish 시간, 인스톨러 크기, exit code)
    - [AUDIT] 로그 샘플 인용 (3-5 라인)
    - Phase 7 종료 선언 + 다음 단계 (RETROSPECTIVE / 다음 milestone)
  </behavior>
  <action>
1. `.planning/phases/07-json-저장-결함수정/07-03-SUMMARY.md` 작성. 템플릿 (`$HOME/.claude/get-shit-done/templates/summary.md`) 의 표준 섹션을 따른다.

2. 필수 섹션:
   - **Objective recap**: Phase 7 목적 + 본 plan 의 verification scope
   - **What was built**: Task 1 (인스톨러) + Task 2/3/4 (검증 활동)
   - **Verification results**:
     - FUNC-11 (DF-2-05) 시나리오 A-1..A-4 결과
     - FUNC-12 (DF-2-06) 시나리오 B-1..B-4 결과
     - 일반 회귀 시나리오 C-1, C-2 결과
     - 1.0.2 → 1.0.3 in-place 업그레이드 U-1..U-5 결과
     - 감사 로그 샘플 (3-5 [AUDIT] 라인)
   - **Phase 7 success criteria 매핑** (ROADMAP.md):
     - 1. FUNC-11 ✓ — 좌측 모드 버튼 동기화 (07-01 + 본 plan A-3)
     - 2. FUNC-12 ✓ — 1-BBOX 저장 차단 (07-02 + 본 plan B-1)
     - 3. 회귀 ✓ — v1.0.2 통과 시나리오 미회귀 (본 plan C-1, C-2)
     - 4. 인스톨러 + 업그레이드 ✓ (본 plan Task 1 + U-1..U-5)
   - **Build log excerpt**: build-installer.ps1 출력 핵심 라인 (publish 시간, ISCC, 인스톨러 크기)
   - **Files modified**: 07-01 (Forms/MainForm.cs SetMode), 07-02 (Forms/MainForm.cs 1-BBOX 가드), 본 plan (installer/Output/ASLT-Setup-v1.0.3.exe)
   - **Files NOT modified (가드 검증)**: Services/JsonService.cs (D-05 준수)
   - **Next steps**: `/gsd:transition` Phase 7 → 종료, RETROSPECTIVE 작성, 다음 milestone 정의 (`/gsd:new-milestone`) — DOC-01/02/03 + USAB-05 + AVI cold-start

3. 결함 비재현 + 회귀 0 + 업그레이드 통과 시 마지막에 명시:
   ```
   **Phase 7 결과:** KTC 2차 결함 2건 (DF-2-05 / DF-2-06) 0건화 확인. v1.0.3 인스톨러 production-ready.
   ```
  </action>
  <verify>
    <automated>
      # 1. SUMMARY 파일 존재
      ls C:/Users/ANNA/AOLTv1.0/.planning/phases/07-json-저장-결함수정/07-03-SUMMARY.md
      # 2. 핵심 토큰 포함 (모든 시나리오 ID 검사)
      grep -c "FUNC-11\|FUNC-12\|DF-2-05\|DF-2-06" .planning/phases/07-json-저장-결함수정/07-03-SUMMARY.md   # >= 4
      grep -c "A-1\|A-2\|A-3\|A-4\|B-1\|B-2\|B-3\|B-4\|C-1\|C-2\|U-1\|U-2\|U-3\|U-4\|U-5" .planning/phases/07-json-저장-결함수정/07-03-SUMMARY.md    # >= 14 (모든 시나리오 인용)
      grep -c "ASLT-Setup-v1.0.3.exe" .planning/phases/07-json-저장-결함수정/07-03-SUMMARY.md   # >= 1
      grep -c "AUDIT" .planning/phases/07-json-저장-결함수정/07-03-SUMMARY.md   # >= 1 (로그 샘플 인용)
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] 07-03-SUMMARY.md 작성 완료
    - [ ] FUNC-11/FUNC-12/DF-2-05/DF-2-06 모두 인용됨
    - [ ] Task 2/3 의 14개 시나리오 결과 모두 표기 (A-1..A-4, B-1..B-4, C-1, C-2, U-1..U-5)
    - [ ] Phase 7 ROADMAP success criteria 4건 명시적 매핑
    - [ ] 빌드 로그 핵심 발췌 (publish 시간 / 인스톨러 크기)
    - [ ] [AUDIT] 로그 샘플 인용 (정상 저장 케이스 3-5 라인)
    - [ ] Services/JsonService.cs 미변경 명시
    - [ ] Next steps 에 RETROSPECTIVE + 다음 milestone (`/gsd:new-milestone`) 안내
  </acceptance_criteria>
  <done>
    07-03-SUMMARY.md 가 모든 verification 결과를 보존하고 Phase 7 클로즈아웃을 명확히 함. 인증 감사 트레이스 + retrospective 입력으로 즉시 사용 가능.
  </done>
</task>

</tasks>

<verification>
**자동 검증** (각 task verify 블록 참조)

**전체 Phase 7 클로즈아웃 체크리스트:**
- [ ] 07-01-SUMMARY.md / 07-02-SUMMARY.md / 07-03-SUMMARY.md 모두 존재
- [ ] FUNC-11 acceptance criteria 4건 모두 통과 (REQUIREMENTS.md)
- [ ] FUNC-12 acceptance criteria 4건 모두 통과 (REQUIREMENTS.md)
- [ ] Phase 7 ROADMAP success criteria 4건 모두 통과
- [ ] ASLT-Setup-v1.0.3.exe production-ready
- [ ] 1.0.2 → 1.0.3 in-place 업그레이드 성공
- [ ] 감사 로그 + HMAC chain 무결성 정상
- [ ] Services/JsonService.cs 미수정 (D-05 가드)
- [ ] 새 기능 추가 0 (PROJECT.md 원칙 준수)
- [ ] Out of scope 항목 미터치 (DOC, USAB-05, AVI cold-start, HumanUAT)

**Out of scope 명시:**
- DOC-01/02/03 — 다음 milestone
- USAB-05 — 다음 milestone
- AVI cold-start 정밀 조사 — 다음 milestone
- HumanUAT 잔여 — 다음 milestone
</verification>

<success_criteria>
- [ ] ASLT-Setup-v1.0.3.exe 가 installer/Output/ 에 존재 + 정상 실행
- [ ] 14개 수동 verification 시나리오 모두 pass
- [ ] [AUDIT] 로그 정상 + HMAC chain 무결성 유지
- [ ] 07-03-SUMMARY.md 가 인증 감사 트레이스 형태로 완성
- [ ] CONTEXT.md D-11, D-12 모두 준수 (3 plan 분할 + build-installer.ps1 사용)
</success_criteria>

<output>
완료 후 `.planning/phases/07-json-저장-결함수정/07-03-SUMMARY.md` 작성 (Task 5).
이후 사용자에게 `/gsd:transition` (Phase 7 종료) + `/gsd:new-milestone` (DOC + 인증 신청 직전 정리) 안내.
</output>
