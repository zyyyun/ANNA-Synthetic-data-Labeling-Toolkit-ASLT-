---
phase: 07-json-저장-결함수정
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - Forms/MainForm.cs
autonomous: true
requirements:
  - FUNC-12
tags:
  - winforms
  - json-export
  - defect-fix
  - waypoint
must_haves:
  truths:
    - "BBOX 1개만 존재하는 객체가 있고 해당 객체에 매칭되는 Waypoint 가 없으면 JSON 저장이 차단되고 안내 메시지가 표시된다 (Entry==Exit Waypoint 0건 보장)"
    - "사용자에게 표시되는 안내 메시지 톤이 기존 수동 Exit 안내 ('Exit 프레임은 Entry 프레임보다 뒤에 있어야 합니다.') 와 일관된다"
    - "정상 케이스 (모든 객체에 BBOX ≥ 2개 또는 명시적 Waypoint) 는 동작 변경 없이 정상 저장된다 (회귀 0)"
    - "btnExportJson 헤더 버튼 + btnExportJsonInLabels 라벨 그룹박스 버튼 양쪽 진입점이 동일하게 보호된다 (Designer.cs:138, 721 양쪽)"
  artifacts:
    - path: "Forms/MainForm.cs"
      provides: "btnExportJson_Click 의 1-BBOX 가드 — boundingBoxes 를 (Label, GetBoxId) 로 group → Waypoint 매칭 없는 단일 BBOX 그룹 검출 시 MessageBox 표시 후 early return"
      contains: "Entry 이후 프레임"
  key_links:
    - from: "btnExportJson_Click (line 716)"
      to: "1-BBOX 가드 분기 (기존 0-BBOX 가드 직후, line 768 직후)"
      via: "in-method guard"
      pattern: "// FUNC-12"
    - from: "1-BBOX 검출 로직"
      to: "MessageBox.Show + return"
      via: "early return"
      pattern: "MessageBox.Show.*Entry 이후 프레임"
---

<objective>
FUNC-12 (KTC DF-2-06) 결함 수정: BBOX 가 1개만 존재하는 상태에서 JSON 저장을 시도할 때 JsonService 가 Entry==Exit 인 Waypoint 를 무음으로 생성하던 동작을 차단한다.

`btnExportJson_Click` (Forms/MainForm.cs:716) 의 기존 0-BBOX 가드 (line 729-768) 직후, 저장 다이얼로그 표시 직전에 1-BBOX 가드를 추가한다. `boundingBoxes` 를 `(Label, GetBoxId(box))` 키로 group 하여, sameObjectBoxes 가 1개이면서 매칭되는 Waypoint 가 없는 그룹이 하나라도 있으면 안내 메시지 후 early return.

Purpose: KTC DF-2-06 결함 0건화. JsonService.cs (Import/Export 로직) 변경 없이 UI 측에서 차단 — 사용자 흐름 일관성 확보 (수동 Exit 안내 메시지와 톤 매칭).

Output: Forms/MainForm.cs 의 btnExportJson_Click 에 가드 분기 추가. JsonService.cs 수정 없음 (D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-json-저장-결함수정/07-CONTEXT.md
@CLAUDE.md
@Forms/MainForm.cs
@Services/JsonService.cs

<truths>
- 새 기능 추가 금지 — 결함 수정만
- Services/JsonService.cs 수정 금지 (CONTEXT.md D-05) — Import/Export 로직 변경 없음
- 본 plan 은 07-01 (SetMode) 완료 후 실행 — 같은 파일 (MainForm.cs) 의 다른 region 을 수정하므로 wave 2
- ISO/IEC 25023 8대 품질 특성 충족 / KISA 보안 가이드 준수
- 안내 메시지는 한국어, MessageBoxIcon.Warning 사용 (기존 수동 Exit 안내와 톤 매칭)
- btnExportJson 와 btnExportJsonInLabels 두 버튼이 동일 핸들러에 연결됨 (Designer.cs:138, 721) — 한 곳 수정으로 양쪽 보호
</truths>

<interfaces>
<!-- Executor 가 그대로 사용해야 할 기존 시그니처 / 코드 모양 -->

From Services/JsonService.cs:48 (정적 헬퍼 — MainForm 에서도 자체 private GetBoxId 가 line 3226 에 존재. 두 GetBoxId 는 동일 시그니처/동작):
```csharp
public static int GetBoxId(BoundingBox box)
{
    if (box.Label == "person") return box.PersonId;
    if (box.Label == "vehicle") return box.VehicleId;
    if (box.Label == "event") return box.EventId;
    return 0;
}
```

From Forms/MainForm.cs:3226 (MainForm 내부에서 사용되는 unqualified GetBoxId 호출은 이 private 메서드로 resolve):
```csharp
private int GetBoxId(BoundingBox box)
```

From Forms/MainForm.cs:716-768 (가드 추가 위치 — 기존 0-BBOX 가드 종료 직후):
```csharp
private async void btnExportJson_Click(object sender, EventArgs e)
{
    try
    {
        if (string.IsNullOrEmpty(_videoService.CurrentVideoFile))
        {
            MessageBox.Show("먼저 비디오 파일을 로드해주세요.", "경고", MessageBoxButtons.OK, MessageBoxIcon.Warning);
            return;
        }

        // DF-1-04 (D-08): Waypoint/BBOX 없는 상태에서의 저장 시도 처리 ...
        if (boundingBoxes.Count == 0 && waypointMarkers.Count == 0)
        {
            // ... (line 729-768, 0-BBOX 가드 + early return)
        }

        // ← 여기 (line 768 직후) 1-BBOX 가드 삽입

        Form loadingForm = new Form { ... };  // line 770~ 저장 다이얼로그
```

From Forms/MainForm.cs:1100, 1157 (기존 한국어 안내 메시지 톤 — Entry/Exit 가드용):
```csharp
MessageBox.Show("Exit 프레임은 Entry 프레임보다 뒤에 있어야 합니다.", "Warning", MessageBoxButtons.OK, MessageBoxIcon.Warning);
```

From Forms/MainForm.cs:1196 (기존 multi-line 안내 메시지 패턴 참고):
```csharp
string mismatchMsg = "Entry-Exit 구간 내 객체 ID가 일치하지 않습니다:\n\n" +
    "...\n\n해결 방법: Ctrl+N 또는 단축키로 ...";
```

From Models/BoundingBox.cs (필드 — IsDeleted 는 삭제 마커, 가드에서 제외해야 함):
```csharp
public class BoundingBox {
    public int FrameIndex;
    public Rectangle Rectangle;
    public string Label;     // "person" | "vehicle" | "event"
    public int PersonId;
    public int VehicleId;
    public int EventId;
    public bool IsDeleted;
    // ...
}
```

From Models/WaypointMarker.cs (필드 — 매칭 키):
```csharp
public class WaypointMarker {
    public int ObjectId;
    public string Label;
    public int EntryFrame;
    public int ExitFrame;
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: btnExportJson_Click 1-BBOX 가드 추가</name>
  <files>Forms/MainForm.cs</files>
  <read_first>
    - .planning/phases/07-json-저장-결함수정/07-CONTEXT.md (전체 — 특히 D-05..D-10)
    - Forms/MainForm.cs:712-770 (JSON Export region 시작 + 기존 0-BBOX 가드 — 가드 위치/톤의 모범)
    - Forms/MainForm.cs:1095-1110 (기존 "Exit 프레임은 Entry 프레임보다 뒤에" 안내 메시지 톤)
    - Forms/MainForm.cs:1192-1200 (기존 multi-line 안내 메시지 패턴 — "해결 방법:" 형식)
    - Forms/MainForm.cs:3220-3240 (MainForm 내부 GetBoxId — unqualified 호출 시 resolve 되는 메서드)
    - Services/JsonService.cs:505-555 (Export 측 sameObjectBoxes / Waypoint 매칭 로직 — 가드가 막는 동작의 발생 지점, 변경 금지)
    - Forms/MainForm.Designer.cs:138 (btnExportJson) + 721 (btnExportJsonInLabels) — 두 버튼이 동일 핸들러 사용 확인
  </read_first>
  <behavior>
    - 정상 케이스 (모든 객체에 BBOX ≥ 2개): 가드 통과 → 기존 저장 다이얼로그 표시 → 정상 저장 (회귀 0)
    - 명시적 Waypoint 가 있는 객체 (BBOX 1개여도 사용자가 수동 Entry/Exit 설정): 가드 통과 → 정상 저장
    - BBOX 0개 + Waypoint 0개: 기존 0-BBOX 가드가 처리 (변경 없음)
    - **1-BBOX 가드 발동 케이스**: 어떤 (Label, GetBoxId) 그룹의 BBOX 가 정확히 1개 + 해당 객체를 커버하는 Waypoint (`w.Label == box.Label && w.ObjectId == GetBoxId(box) && box.FrameIndex >= w.EntryFrame && box.FrameIndex <= w.ExitFrame`) 가 없으면 → MessageBox 표시 + early return + 저장 다이얼로그 미표시
    - 메시지 본문에 결함 객체의 Label + ID 가 포함되어 사용자가 어느 객체를 수정해야 하는지 즉시 식별 가능
    - IsDeleted == true 인 박스는 그룹핑에서 제외
  </behavior>
  <action>
1. **가드 삽입 위치 확정** — Forms/MainForm.cs:716 의 `btnExportJson_Click` 메서드 본문에서 기존 0-BBOX 가드 (line 729-768) 가 끝난 직후, 저장 다이얼로그 (`Form loadingForm = new Form` line 770) 가 시작되기 직전. 즉 line 768 의 `}` (0-BBOX 가드 블록 닫는 중괄호) 와 line 770 의 빈 줄 사이.

2. **가드 코드 — 그대로 삽입** (CONVENTIONS.md 톤 + 기존 0-BBOX 가드 패턴 매칭):

```csharp
                // FUNC-12 (DF-2-06): BBOX 1개만 존재하는 객체가 Waypoint 없이 저장되는 경우
                // — JsonService 가 Entry==Exit 인 Waypoint 를 생성하여 사용자 안내 메시지
                //   ("Exit 는 Entry 이후 프레임에서 설정") 와 모순되는 흐름을 막는다.
                //   해당 객체에 명시적 Waypoint 가 있으면 통과 (사용자 의도 존중).
                var unfinishedTracks = boundingBoxes
                    .Where(b => !b.IsDeleted)
                    .GroupBy(b => new { b.Label, ObjectId = GetBoxId(b) })
                    .Where(g =>
                    {
                        var sameObjectBoxes = g.ToList();
                        if (sameObjectBoxes.Count != 1) return false;
                        var only = sameObjectBoxes[0];
                        bool hasMatchingWaypoint = waypointMarkers.Any(w =>
                            w.Label == only.Label &&
                            w.ObjectId == GetBoxId(only) &&
                            only.FrameIndex >= w.EntryFrame &&
                            only.FrameIndex <= w.ExitFrame);
                        return !hasMatchingWaypoint;
                    })
                    .Select(g => new { g.Key.Label, g.Key.ObjectId })
                    .ToList();

                if (unfinishedTracks.Count > 0)
                {
                    string offending = string.Join(", ",
                        unfinishedTracks.Select(t => $"[{t.Label}] [{t.ObjectId:D2}]"));
                    MessageBox.Show(
                        "Exit 는 Entry 이후 프레임에서 설정해주세요. " +
                        "하나의 BBOX 만 존재하는 객체가 있어 Waypoint 를 생성할 수 없습니다.\n\n" +
                        $"해당 객체: {offending}\n\n" +
                        "해결 방법: 해당 객체의 다른 프레임에서 BBOX 를 추가하거나, " +
                        "Entry/Exit 를 명시적으로 설정한 후 다시 저장해주세요.",
                        "JSON 저장 차단",
                        MessageBoxButtons.OK,
                        MessageBoxIcon.Warning);
                    return;
                }
```

3. **GetBoxId 호출 형태 확인** — `btnExportJson_Click` 스코프에서 `GetBoxId(b)` 는 MainForm 의 private 메서드 (line 3226) 로 resolve 됨. JsonService.GetBoxId 와 동일 동작이므로 unqualified 호출 그대로 사용.

4. **using 추가 불필요** — `System.Linq` 는 line 5 에서 이미 import.

5. **메시지 톤 매칭 — 핵심 검증 항목**:
   - 첫 문장: "Exit 는 Entry 이후 프레임에서 설정해주세요." → 기존 line 1100 "Exit 프레임은 Entry 프레임보다 뒤에 있어야 합니다." 와 동일 의도/톤 (REQUIREMENTS.md FUNC-12 acceptance: "Exit 는 Entry 이후 프레임에서 설정" 안내 메시지와 일관된 흐름)
   - 객체 식별: `[{Label}] [{ObjectId:D2}]` 형식 — 기존 line 1182 의 `Person ID {entryBox.PersonId:D2}` 패턴과 동일 자릿수 포맷
   - "해결 방법:" 섹션 — 기존 line 1198 패턴과 일치
   - MessageBoxIcon.Warning + MessageBoxButtons.OK — 0-BBOX 가드(line 740 Warning) 와 일관

6. **JsonService.cs 변경 금지** — D-05 / Out of scope 가드. line 543-552 (sameObjectBoxes) 의 entry/exit 결정 로직은 정상 케이스(BBOX ≥ 2개 + min/max 다름)에서만 호출되므로 본 가드 추가 후 안전.

7. **Designer.cs 양쪽 진입점** — btnExportJson (line 138) + btnExportJsonInLabels (line 721) 모두 같은 핸들러를 호출 → 가드는 자동으로 두 버튼 모두에 적용. Designer.cs 수정 없음.

8. **try-catch 구조 유지** — 가드는 기존 outer `try { ... }` (line 718) 블록 내부에 위치. 예외 흐름 변경 없음.
  </action>
  <verify>
    <automated>
      # 1. 빌드 성공
      dotnet build C:/Users/ANNA/AOLTv1.0/ASLTv1.0.csproj -c Debug -v minimal
      # 2. FUNC-12 가드 핵심 토큰 검출
      grep -c "FUNC-12 (DF-2-06)" Forms/MainForm.cs                        # >= 1
      grep -c "unfinishedTracks" Forms/MainForm.cs                         # >= 2 (선언 + 사용)
      grep -c "Exit 는 Entry 이후 프레임에서 설정" Forms/MainForm.cs       # >= 1
      grep -c "하나의 BBOX 만 존재하는 객체가 있어" Forms/MainForm.cs       # >= 1
      grep -c "JSON 저장 차단" Forms/MainForm.cs                            # >= 1
      # 3. 가드 위치 — 메서드 단위로 awk 추출은 try/catch 닫는 brace 와 충돌하므로 사용하지 않음. 위 (2) 의 글로벌 토큰 카운트가 동일 의도 검증.
      # 4. JsonService.cs 미변경
      git diff --stat Services/JsonService.cs    # 빈 출력 (변경 없음)
      # 5. 헤더 + 라벨 그룹박스 양쪽 진입점이 여전히 동일 핸들러 사용
      grep -c "btnExportJson_Click" Forms/MainForm.Designer.cs    # == 2
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] `dotnet build` 성공 (warning 0 증가, error 0)
    - [ ] btnExportJson_Click 에 1-BBOX 가드 분기 추가됨 — 기존 0-BBOX 가드 직후 위치
    - [ ] 가드 로직: `boundingBoxes.Where(!IsDeleted).GroupBy(Label, GetBoxId)` + sameObjectBoxes.Count == 1 + Waypoint 매칭 없음 검사
    - [ ] Waypoint 매칭 조건: `w.Label == box.Label && w.ObjectId == GetBoxId(box) && FrameIndex in [EntryFrame, ExitFrame]`
    - [ ] 가드 발동 시 MessageBox 메시지에 "Exit 는 Entry 이후 프레임에서 설정" 문구 포함
    - [ ] 메시지에 결함 객체 식별자 (`[Label] [ID]`) 표시
    - [ ] 메시지 caption "JSON 저장 차단", icon Warning, buttons OK
    - [ ] 가드 발동 시 early return — 저장 다이얼로그 미표시
    - [ ] IsDeleted == true 인 박스는 그룹핑에서 제외
    - [ ] Services/JsonService.cs 변경 없음 (`git diff --stat` 비어있음)
    - [ ] Forms/MainForm.Designer.cs 변경 없음 (양쪽 진입점이 자동 보호됨)
    - [ ] 한국어 주석 prefix `// FUNC-12 (DF-2-06):` 사용 (기존 `// DF-1-04 (D-08):` 패턴 일관)
  </acceptance_criteria>
  <done>
    빌드 성공 + 모든 acceptance criteria 통과. 1-BBOX 케이스에서 저장이 차단되고 안내 메시지가 표시됨. 정상 케이스 (BBOX ≥ 2개) 는 회귀 없음. 통합 회귀 테스트는 07-03 plan 에서 실행.
  </done>
</task>

</tasks>

<verification>
**자동 검증** (Task 1 verify 블록 참조)

**수동 회귀 검증** (이 plan 종료 시점에는 빌드 성공 + 메시지 핵심 토큰 검출만으로 충분 — 통합 회귀는 07-03):
1. 디버그 빌드 후 ASLTv1.exe 실행 + 영상 1개 로드
2. 임의의 한 프레임에서 person BBOX 1개만 그림 → JSON 저장 메뉴 호출 → "Exit 는 Entry 이후 프레임에서 설정해주세요. ... 해당 객체: [person] [01]" 메시지 표시 + 저장 다이얼로그 미표시 확인
3. 같은 객체에 다른 프레임에서 BBOX 1개 추가 (총 2개) → JSON 저장 → 정상 저장 확인 (회귀 없음)
4. BBOX 0개 + Waypoint 0개 + 기존 JSON 있는 상태에서 저장 → 기존 0-BBOX 가드 (삭제 프롬프트) 동작 확인 (변경 없음)
5. 사용자가 명시적 Entry/Exit 설정 후 BBOX 1개 + Waypoint 1개 → 저장 → 정상 통과 확인 (사용자 의도 존중)

**Out of scope:** 모드 버튼 시각 (FUNC-11 → 07-01 plan), 인스톨러 빌드 (07-03 plan)
</verification>

<success_criteria>
- [ ] FUNC-12 acceptance criteria 4개 (REQUIREMENTS.md + CONTEXT.md 추가) 모두 충족
- [ ] Forms/MainForm.cs 외 파일 변경 0
- [ ] 빌드 warning 0 증가
- [ ] CONTEXT.md D-05..D-10 모두 반영 (저장 차단 / btnExportJson_Click 위치 / GroupBy 로직 / 메시지 톤 매칭 / 정상 케이스 회귀 0 / DF-1-04 패턴 따름)
</success_criteria>

<output>
완료 후 `.planning/phases/07-json-저장-결함수정/07-02-SUMMARY.md` 작성:
- 변경 파일/라인 요약 (btnExportJson_Click 추가 분기 인용)
- 가드 로직 의사코드 (GroupBy + sameObjectBoxes.Count == 1 + Waypoint 매칭 없음)
- 표시되는 메시지 본문 전문
- 빌드 로그 요약
- FUNC-12 acceptance criteria 체크리스트 결과
</output>
