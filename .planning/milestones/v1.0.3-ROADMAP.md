# Milestone v1.0.3: JSON 저장 결함 수정 (KTC 2차)

**Status:** ✅ SHIPPED 2026-05-06
**Phases:** 7 (single phase)
**Total Plans:** 3 (07-01, 07-02, 07-03)
**Final Build:** ASLT-Setup-v1.0.3.exe (98.19 MB, SHA256 `2072B5B54AA1417DAFBD91219DFF3B8A7923B67987D1E51559DF06CB66E61698` — 4차 final, ships v1.0.3)
**Timeline:** 2026-05-06 (single-day cycle)
**Commits:** 14 on main (3 source + 11 docs/chore)

## Overview

KTC 2차 결함보고서에서 식별된 JSON 저장 관련 결함 2건 (DF-2-05, DF-2-06) 을 수정하기 위한 patch milestone. v1.0 의 Phase 5.6 (KTC 1차 결함수정) 패턴과 일관되게 인증 감사 트레이스를 유지하기 위해 quick task 가 아닌 별도 milestone 으로 처리.

UAT 15/15 시나리오 통과 (사용자 보고). 사용자 흐름 개선 사항 2건이 동일 v1.0.3 binary 에 통합 빌드됨 (정보 팝업 Entry/Exit Shift 변형 표기, 로그 보존 30일 → 180일).

## Phases

### Phase 7: JSON 저장 결함수정 (KTC 2차)

**Goal**: KTC 2차 결함보고서에서 식별된 DF-2-05, DF-2-06 을 정확히 재현 → 근본 원인 분석 → 수정 → 회귀 테스트 후 v1.0.3 인스톨러 빌드 + 1.0.2 → 1.0.3 in-place 업그레이드 검증.
**Depends on**: v1.0 (Phase 5.6 KTC 1차 결함수정 완료 상태)
**Plans**: 3 plans

Plans:

- [x] **07-01**: FUNC-11 (DF-2-05) — `SetMode(DrawMode)` 헬퍼 도입 + 모드 버튼 시각 동기화 (commit `18f3126`, 2026-05-06)
- [x] **07-02**: FUNC-12 (DF-2-06) — `btnExportJson_Click` 1-BBOX 가드 추가 (commit `9eb6940`, 2026-05-06)
- [x] **07-03**: 회귀 테스트 + ASLT-Setup-v1.0.3.exe 빌드 (3차 빌드) + 1.0.2 → 1.0.3 in-place 업그레이드 검증 + audit log 무결성 (commit `4228037`, 2026-05-06)

**Success Criteria (all met):**
1. ✅ FUNC-11 — JSON 저장 메뉴 호출 시 좌측 모드 버튼이 '전체선택' 활성 표시로 동기화 (편집모드 잔존 표시 0건). UAT A-3, A-5 통과.
2. ✅ FUNC-12 — BBOX 1개 상태에서 JSON 저장 시 Entry==Exit Waypoint 생성 0건, 안내 메시지와 일관된 흐름. UAT B-1 통과.
3. ✅ 회귀: v1.0.2 에서 통과한 JSON 저장 시나리오 (타임스탬프, 카테고리 매핑, BBOX ≥ 2개 정상 케이스) 미회귀. UAT B-2/B-3/B-4/C-1/C-2 통과.
4. ✅ v1.0.3 인스톨러 빌드 성공 + 1.0.2 → 1.0.3 in-place 업그레이드 검증 통과. UAT U-2..U-5 통과.

**Details:**

- 1차 빌드 (commit `ab8bb03`, 2026-05-06 13:03:14, SHA256 `08EABFD8...`) — superseded
- 2차 빌드 (commit `6d29324`, 2026-05-06 15:19:02, SHA256 `FBA0B886...`) — superseded
- 3차 빌드 (commit `0724248`, 2026-05-06 17:02:03, SHA256 `9ECAF3C3...`) — superseded (closeout 직후 OnboardingForm Shift 변형 누락 발견)
- 4차 빌드 (commit `f719195` source + 신규 build, 2026-05-07 10:38:46, SHA256 `2072B5B5...`) — **final, ships v1.0.3** — `Forms/OnboardingForm.cs` 시작 가이드 카드 텍스트 일관성 패치 포함
- 옵션 C (1.0.2 가 사용자 환경에 이미 설치) UAT — clean install (U-1) 생략, U-2..U-5 진행
- D-05 가드 100% 준수: `Services/JsonService.cs` 무수정 (Phase 5.6 의 import-side matcher 와 round-trip 일관성 보장)
- 코드 검증: `dotnet build` 0 errors, 39 warnings (베이스라인 동일)

---

## Milestone Summary

**Decimal Phases:**

- 없음 — Phase 7 단일 phase 로 milestone 구성

**Key Decisions (from CONTEXT.md D-01..D-12):**

- **D-01..D-04**: `SetMode(DrawMode mode)` 단일 헬퍼로 `currentMode` + 좌측 버튼 BackColor + 커서 원자적 동기화. ARGB 인라인 상수는 helper 인근의 `private static readonly Color` 필드로 캡슐화. 4개 호출 지점 (`btnSelectAll_Click`, `btnEdit_Click`, `LoadLabelingData` reset @ line 675, `MainForm_Load`) 모두 헬퍼 호출로 통일.
- **D-05**: FUNC-12 fix 를 UI-side guard 로 구현 — `btnExportJson_Click` 의 0-BBOX 가드 직후에 1-BBOX 가드 삽입. JsonService.cs Export/Import 라운드트립 미수정. 이유: round-trip 일관성 + Phase 5.6 의 DF-1-04 0-BBOX 가드 패턴 재사용 + cert audit trace 일관.
- **D-07**: Waypoint 매칭 predicate 가 `JsonService.cs:527-531` 의 import-side matcher 와 1:1 일치 — round-trip 일관성 보장.
- **D-08**: 메시지 톤은 `MainForm.cs:1104` "Exit 프레임은 Entry 프레임보다 뒤에 있어야 합니다." 와 매칭 — USAB-03 사용자 흐름 일관성.
- **D-11**: 3 plan 분할 (FUNC-11 / FUNC-12 / 회귀+인스톨러+업그레이드).
- **D-12**: `installer/build-installer.ps1` 기존 자동화 사용. AppId pinned 유지.
- **3차 빌드 cycle (UAT 도중)**: 사용자 흐름 개선 사항 (정보 팝업 Shift 변형 표기 commit `8a22445`, 로그 보존 30→180일 commit `df890bd`) 을 별도 milestone 으로 미루지 않고 v1.0.3 동일 binary 에 통합. KTC 인증 신청 시 revisit 비용 최소화.
- **4차 빌드 cycle (closeout 직후 v1.0.3 re-open)**: 시작 가이드(`OnboardingForm`) 의 Entry/Exit 단축키 표기에서 Shift 변형 누락이 milestone closeout 직후 발견. v1.0.3 tag 가 미배포 상태였으므로 tag 삭제 → fix commit `f719195` → 재빌드 → tag 재생성 으로 동일 v1.0.3 binary 에 통합. AboutForm 정보 팝업 (commit `8a22445`) 과 일관성 유지.
- **옵션 C UAT**: 사용자 환경 1.0.2 위에 in-place 업그레이드 (인스톨러 백업 N/A) — clean-install 단계 (U-1) 생략, U-2..U-5 진행.
- **Audit log primary evidence gap 처리**: 사용자 closeout 결정 ("마일스톤 마무리") 에 따라 Phase 7 클로즈아웃 진행. Gap 은 RELI-NEW-01 으로 다음 milestone 명시 이월. False-pass 0건, primary evidence (UAT 15/15 + 코드 + 빌드) 와 secondary evidence (audit log) 분리 honest documentation.

**Issues Resolved:**

- KTC 2차 결함보고서 DF-2-05 (FUNC-11) — JSON 저장 후 좌측 모드 버튼 시각 불일치 (편집모드 잔존 표시) 0건화
- KTC 2차 결함보고서 DF-2-06 (FUNC-12) — BBOX 1개 + Waypoint 없음 상태에서 Entry==Exit Waypoint 자동 생성 0건화
- (out-of-scope but applied) 정보 팝업의 Entry/Exit 단축키 표기에서 Shift 변형 누락 — `"E"` → `"E or 'Shift + E'"`, `"X"` → `"X or 'Shift + X'"` (코드와 문서 일치, NEW-03 패턴)
- (out-of-scope but applied) 로그 보존 정책 30일 → 180일 (6개월) — GS인증 감사 트레이스 확보 + 보존 기간 확장

**Issues Deferred (RELI-NEW-01 to next milestone):**

- **RELI-NEW-01**: `btnExportJson_Click` 정상 저장 경로의 `LogService.AuditJsonSave` 호출 회귀 테스트 + 사용자 흐름 audit log E2E 검증 케이스 추가. UAT JSON 저장 활동의 [AUDIT] 엔트리가 audit log 에 부재 (HMAC chain 자체는 정상). KTC 인증 신청 직전에 closure 필요. Phase 7 결함 0건화 자체는 primary evidence 3계층 (UAT 15/15 + 코드 + 빌드) 으로 입증됨 — 본 항목은 secondary evidence gap 의 follow-up.
- DOC-01/02/03 — 제품설명서, 사용자취급설명서, 버전 일치 검증 (인증 신청 전 필수, v1.0 부터 이월)
- USAB-05 — Undo/Redo UI 버튼 결정 (UI 신규 추가 vs 영구 N/A, v1.0 부터 이월)
- AVI cold-start 정밀 조사 — 재현 데이터 확보 후 디코더 워밍업 fix (v1.0 부터 이월)
- HumanUAT 잔여 항목 정리 (v1.0 부터 이월)

**Technical Debt Incurred:**

- Audit log E2E 검증 케이스 부재 — RELI-NEW-01 으로 명시 follow-up.
- ISCC 컴파일러 deprecation 경고 (`Architecture identifier "x64" deprecated` → 권장 `x64compatible`) — `installer/ASLT-Setup.iss` 에서 다음 milestone 시 정리 가능.
- `<Nullable>disable</Nullable>` 환경에서 `CS8632` 경고 다수 — annotations 무해, 별도 처리 안 함.

---

_For current project status, see [.planning/ROADMAP.md](../ROADMAP.md)_
